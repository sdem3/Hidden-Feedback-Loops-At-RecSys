# Скрытые петли обратной связи в рекомендательных системах: математическая модель и экспериментальный анализ

> **Назначение документа.** Это самостоятельный научный отчёт о деградации качества рекомендательных систем под действием скрытой петли обратной связи. Он рассчитан на читателя, не знакомого с темой: все понятия вводятся с нуля, каждый символ объясняется при первом появлении, после каждого формального блока следует содержательная интерпретация. Документ содержит математическую модель, доказательства всех утверждений и сопоставление теории с проведёнными вычислительными экспериментами.
>
> **Главный результат в одной фразе.** Деградация складывается из двух различных каналов: **β-канал** (универсальное стягивание разнообразия вкусов, не зависящее от переобучения) и **петлевой канал** (специфическое для переобучения заострение выдачи и переформирование *ковариации* предпочтений). Первый доказан строго; второй установлен в специальной гауссовой модели и подтверждён экспериментально.
>
> **Три уровня доказанности (читать до формул).** Чтобы не переоценивать результаты, мы всюду разделяем:
> - **доказано строго** — выводится из модели без дополнительных гипотез (механизм на уровне логов, §3; β-канал, §5);
> - **доказано в модели** — выводится в специальной гауссовой модели при явно выписанных модельных допущениях (петлевой канал, §6);
> - **измерено** — установлено экспериментально, с понятным механизмом, но не выведено из первых принципов (§7).
>
> **Рендеринг.** Формулы в нотации `$…$`/`$$…$$` (KaTeX). Шаг переобучения обозначен $\kappa$, чтобы не путать с наблюдаемой вероятностью клика $\eta_t$.

---

## Содержание

- [Аннотация](#аннотация)
- [1. Введение: проблема скрытой петли](#1-введение-проблема-скрытой-петли)
- [2. Обозначения и соглашения](#2-обозначения-и-соглашения)
- [3. Базовый механизм на уровне логов: почему метрики растут, а польза падает](#3-базовый-механизм-на-уровне-логов-почему-метрики-растут-а-польза-падает)
- [4. Динамическая модель петли](#4-динамическая-модель-петли)
- [5. Теорема 1: β-канал стягивает разнообразие](#5-теорема-1-β-канал-стягивает-разнообразие)
- [6. Петлевой канал: кластерный reward и сужение выдачи при переобучении](#6-петлевой-канал-кластерный-reward-и-сужение-выдачи-при-переобучении)
- [7. Эксперименты и их результаты](#7-эксперименты-и-их-результаты)
- [8. Что доказано, что постулировано, что измерено](#8-что-доказано-что-постулировано-что-измерено)
- [9. Итоговая причинная цепь по звеньям](#9-итоговая-причинная-цепь-по-звеньям)
- [10. Прикладные следствия и чек-лист риска](#10-прикладные-следствия-и-чек-лист-риска)
- [11. Границы применимости и дальнейшая работа](#11-границы-применимости-и-дальнейшая-работа)

---

## Аннотация

Промышленные рекомендательные системы периодически переобучаются на логах взаимодействий, которые сами же и порождают. Возникает замкнутый контур, в котором выход системы становится её входом, а наблюдаемые клики перестают быть нейтральным измерением предпочтений. В работе строится математическая модель этого контура и устанавливается, что деградация качества идёт по двум различным каналам. **β-канал** стягивает вкусы пользователей к показанному контенту; он универсален, действует даже без переобучения и отвечает за наблюдаемый коллапс суммарного разброса вкусов (около 97 % потери дисперсии). **Петлевой канал** возникает только при переобучении: модель, дообучаясь на смещённых логах, делает выдачу резче, ковариация показанного сужается, и коллапс вкусов углубляется ниже уровня, достижимого без переобучения.

Для β-канала доказана строгая геометрическая оценка стягивания разброса (Теорема 1). Петлевой канал формализован через **кластерный reward** — средний логируемый сигнал сегмента — и улучшение политики с KL-регуляризацией: локальная вогнутость reward, обоснованная разложением кликовой (BCE) потери, ведёт к монотонному росту резкости выдачи, и ковариация показанного $C=(\Psi^{-1}+A)^{-1}$ при переобучении оказывается по Лёвнеру меньше, чем при замороженной модели (Теорема 2), что опускает шумовой пол β-коллапса; получено проверяемое пороговое условие активации. Предсказания сопоставлены с серией вычислительных экспериментов: универсальность β-канала, локализация петлевого следа в дисперсионной части расхождения $\mathrm{KL}(P_T\Vert P_0)$ и условия активации ($\lVert J_k\rVert\neq0$, число кластеров $K\ge2$, открытая популяция) подтверждены, в том числе на реальных данных; прямой замер сужения выдачи и его границы вынесены в §7.7. Сформулирован практический чек-лист диагностики и подавления петли.

---

## 1. Введение: проблема скрытой петли

### 1.1. Рекомендательная система как замкнутый цикл

Рекомендательную систему обычно представляют как функцию: на вход — пользователь, на выход — список объектов (айтемов). В действительности промышленная система работает иначе. Она почти никогда не обучается однократно: она периодически *переобучается* на свежих логах — кликах, просмотрах, оценках. Но эти логи порождает она сама, потому что пользователь реагирует на то, что ему **показали**, а показ определяется предыдущей версией модели. Возникает замкнутая причинно-следственная цепь:

```
        ┌───────────────────────────────────────────────┐
        │                                               │
        ▼                                               │
   модель  ─→  рекомендации  ─→  клики  ─→  логи  ─→  переобучение
   (шаг t)        (top-L)      (с шумом)              (шаг t+1)
```

Выход на шаге $t$ становится входом на шаге $t+1$. С точки зрения теории это означает, что статическая задача ранжирования превращается в **динамическую систему**: интересует не качество одного списка, а то, куда сходится распределение пользователей и рекомендаций при $t\to\infty$. Скрытая петля обратной связи (hidden feedback loop) — это нежелательные свойства такой эволюции.

### 1.2. Почему петля опасна и почему её трудно заметить

Опасность скрыта в слове «свои». Если бы модель переобучалась на нейтрально собранных данных (например, на честных опросах о вкусах), петли бы не было. Но реальные логи смещены дважды:

1. **Смещение экспозиции.** Пользователь физически не может кликнуть на то, что ему не показали. Каждый лог — уже отфильтрованная моделью выборка.
2. **Смещение следования.** Пользователь кликает на рекомендованное *потому что оно удобно и на виду*, а не потому, что оно лучше всего отвечает его вкусу. Клик перестаёт быть честным измерением предпочтения.

Переобучаясь на таких логах, модель усиливает собственные прошлые решения: то, что она однажды вывела наверх, набирает клики, и эти клики «подтверждают» исходный порядок. Связь с истинными предпочтениями аудитории постепенно теряется.

Самое коварное — что **наблюдаемые метрики при этом растут или держатся высокими**. CTR, AUC, NDCG считаются по тем же смещённым кликам, которые петля и порождает. Система рапортует об успехе ровно тогда, когда деградирует. Поэтому петлю нельзя поймать стандартным A/B-тестом на собственных данных — нужен теоретический критерий, отличающий «модель действительно улучшилась» от «модель замкнулась на себе».

### 1.3. Центральный вопрос: есть ли у петли собственная подпись?

Наивная гипотеза: петля переобучения сама по себе вызывает коллапс разнообразия — все пользователи стягиваются в точку. Однако (как показано в §7) коллапс суммарной дисперсии вкусов происходит **одинаково** и в режиме с переобучением, и в режиме с замороженной моделью, и при переобучении на честных данных; а при отключении дрейфа предпочтений он исчезает во всех режимах. То есть наблюдаемый коллапс — это эффект *движения вкусов при концентрированной выдаче*, а не переобучения. Отсюда уточнённый центральный вопрос:

> **Оставляет ли петля переобучения собственный, отделимый от универсального дрейфа след — и если да, в какой геометрической характеристике распределения он измерим?**

### 1.4. Два канала деградации

Ответ работы — в разделении **двух механизмов**, которые наивная интуиция сливала.

- **β-канал (Теорема 1) — универсальный.** Каждый клик слегка сдвигает вкус пользователя к потреблённому контенту: $u^{t+1}=(1-\beta)u^{t}+\beta v$. Если выдача концентрирована, вкусы стягиваются к общим точкам, и разброс убывает геометрически. Канал работает в любом режиме с дрейфом; переобучение для него не требуется.

- **Петлевой канал (Теорема 2) — специфичный для переобучения.** Переобучаясь на смещённых логах, модель делает выдачу **резче**; ковариация показанного $C$ сжимается. Это опускает шумовой пол β-коллапса, и вкусы стягиваются глубже и анизотропнее, чем без переобучения. Канал активируется только если модель *различает пользователей*, и проявляется не в смещении центра аудитории, а в **переформировании ковариации** — в дисперсионной части дивергенции $\mathrm{KL}(P_T\Vert P_0)$.

Главный тезис: *петля имеет собственную сигнатуру, но измерять её надо не в объёме разброса (там доминирует β-канал), а в форме ковариации.*

### 1.5. Вклад и структура отчёта

Документ: (1) формализует уровень логов и объясняет завышение метрик (§3); (2) доказывает β-канал (§5); (3) строит специальную гауссову модель петлевого канала, в которой переобучение как улучшение политики заостряет выдачу, углубляя коллапс ниже статического пола, и даёт проверяемый порог активации (§6); (4) сопоставляет все предсказания с экспериментами и абляциями, включая прямую проверку заострения выдачи (§7); (5) сводит статус каждого утверждения и формулирует практический чек-лист (§8, §10). Технический инструментарий (§2) и границы применимости (§11) вынесены отдельно.

---

## 2. Обозначения и соглашения

| Символ | Смысл |
|--------|-------|
| $\mathcal U,\ \mathcal I,\ d$ | множества пользователей и айтемов; размерность латентного пространства. |
| $N,\ M$ | число пользователей и айтемов. |
| $u_i^t\in\mathbb R^d$ | вектор предпочтений (эмбеддинг) пользователя $i$ на шаге $t$. |
| $v_j\in\mathbb R^d$ | фиксированный вектор айтема $j$ (каталог конечен и неизменен). |
| $\langle u,v\rangle$ | скалярное произведение, $\sum_a u_a v_a$ — мера близости вкуса и айтема. |
| $\sigma(z)=\dfrac{1}{1+e^{-z}}$ | сигмоида (превращает близость в вероятность). |
| $\theta(u,v)=\sigma(\langle u,v\rangle/\tau)$ | **истинная** вероятность отклика (мир без рекомендателя); $\tau>0$ — температура. |
| $f_t(u,v),\ w^t$ | предсказание модели на шаге $t$ и её параметры. |
| $\eta_t(u,v)$ | **наблюдаемая** (логируемая) вероятность клика. |
| $L$ | длина списка рекомендаций (top-$L$); не путать с числом кластеров $K$. |
| $K,\ C_k,\ n_k,\ \pi_k=n_k/N$ | число кластеров; кластер $k$, его размер и вес. |
| $\bar x_k^t$ | выборочный центр (центроид) кластера $k$. |
| $\Sigma_k^t$ | выборочная ковариация эмбеддингов кластера (вокруг подвижного $\bar x_k^t$); $\operatorname{tr}\Sigma_k^t=D_k(t)$. |
| $D_k(t),\ D(t)=\sum_k\pi_k D_k(t)$ | внутрикластерный разброс и его среднее по популяции. |
| $\bar v_k$ | фиксированная «точка притяжения» (мишень) кластера $k$. |
| $M_k(t)=\frac1{n_k}\sum_i\lVert u_i^t-\bar v_k\rVert^2$ | средний квадрат отклонения от мишени; $M_k\ge D_k$. |
| $\beta\in(0,1)$ | сила β-дрейфа (клик тянет вкус к показанному). |
| $\rho\in[0,1]$ | доля активных пользователей за шаг. |
| $s\in[0,1]$ | adherence — степень следования рекомендациям. |
| $p\in[0,1]$ | usage — доля предсказаний, попадающих обратно в логи. |
| $\alpha=ps\in[0,1]$ | совокупная сила петли (загрязнение логов). |
| $r\in[0,1)$ | доля популяции, обновляемая за шаг (приход/уход). |
| $\sigma_{\mathrm{in}}^2=\max_k\operatorname{tr}\Sigma_k^0$ | внутренний разброс кластеров. |
| $\Delta_{\min}=\min_{k\ne l}\lVert\bar x_k^0-\bar x_l^0\rVert$ | зазор между центрами кластеров. |
| $\sigma_\xi^2,\ \Sigma_\xi^t$ | (скалярный и матричный) разброс показанного контента вокруг мишени. |
| $\mu_k,\ S_{k,t}$ | центр и параметрическая ковариация вкусов в гауссовой модели, $U\mid Z=k\sim\mathcal N(\mu_k,S_{k,t})$. |
| $\Psi_k\succ0,\ \nu_k=\mathcal N(\mu_k,\Psi_k)$ | ковариация и распределение каталога айтемов. |
| $r_A(v),\ A_{k,t}\succeq0$ | квадратичный скор и матрица **резкости** (precision) выдачи. |
| $C_{k,t}=(\Psi_k^{-1}+A_{k,t})^{-1}$ | **ковариация выдачи** (показанного контента); в центрированной модели $C_{k,t}=\Sigma_\xi^t$. |
| $c_{k,t}(v)=\mathbb E_{U\sim P_{k,t}}[\eta_t(U,v)]$ | **кластерный reward** айтема $v$ — средний логируемый сигнал кластера; $\widehat c_{k,t}$ — его квадратичное (Тейлор) приближение. |
| $m_{k,t},\ g_{k,t},\ H_{k,t}\succeq0$ | центр выдачи, градиент $\nabla_v c_{k,t}(m_{k,t})$ и кривизна $-\nabla_v^2 c_{k,t}(m_{k,t})$ кластерного reward. |
| $s_w(u,v),\ \widehat p_w=\sigma(s_w)$ | скор модели и предсказанная вероятность отклика (двухбашенный/метрический); BCE-микрообоснование §6.7. |
| $\kappa>0$ | шаг переобучения (step size политики). |
| $J_k=\frac1{n_k}\sum_i\nabla_u\hat r_w\big\rvert_{u_i^t}$ | кластерный градиент скоринга по пользователю; $\lVert J_k\rVert\neq0$ — модель различает пользователей. |
| $\mathcal D(A\Vert B)=\tfrac12(\operatorname{tr}(B^{-1}A)-d+\ln\det B-\ln\det A)$ | дисперсионное расхождение двух p.d. матриц (дисперсионная часть гауссова $\mathrm{KL}$). |
| $T,\ T_{\mathrm{ret}}$ | горизонт симуляции; период переобучения. |
| $\operatorname{tr},\ \det,\ \succeq,\ \preceq$ | след, определитель, порядок Лёвнера. |

**Порядок Лёвнера.** Для симметричных $X,Y$: $X\succeq0$ означает $z^\top Xz\ge0$ для всех $z$; $X\succeq Y$ означает $X-Y\succeq0$ — «$X$ не меньше $Y$ по всем направлениям сразу», что строго сильнее сравнения следов.

**Два «usage» — не путать.** Параметр $p$ (usage) описывает долю *предсказаний*, возвращающихся в обучение, — это загрязнение логов (§3). Параметр $\rho$ описывает долю *активных пользователей* за шаг — это скорость дрейфа векторов (§4–5). Сила петли в смысле логов есть $\alpha=ps$. Скорость геометрического сжатия векторов определяется только $\beta$ и $\rho$ (множитель $q=1-\beta\rho(2-\beta)$, §5); adherence $s$ входит лишь косвенно — через концентрацию выдачи, то есть через шумовой пол.

**Два ковариационных объекта — не смешивать.** (1) $\Sigma_k^t$ — выборочная ковариация *вкусов* пользователей (вокруг подвижного центроида); для строгой рекурсии §5 используется второй момент вкусов вокруг фиксированной мишени $\bar v_k$, его след — $M_k(t)$. (2) $C_{k,t}=\Sigma_\xi^t$ — ковариация *выдачи* (показанного контента), центральный объект петлевого канала (§6). Петля действует на $C$ (сужает её), а через шумовой пол β-коллапса это углубляет сжатие самих вкусов $\Sigma_k$.

### Технический инструментарий (леммы A–D)

> При первом чтении этот раздел можно пропустить и вернуться к нему при разборе §6. Здесь собраны четыре стандартных факта, на которые опираются выкладки.

**Лемма A (произведение гауссовых ядер; завершение квадрата).** Пусть $P,Q\succeq0$, $P+Q\succ0$, $a,b\in\mathbb R^d$. Тогда как функции от $v$
$$
\exp\!\Big(-\tfrac12(v-a)^\top P(v-a)\Big)\exp\!\Big(-\tfrac12(v-b)^\top Q(v-b)\Big)\ \propto\ \exp\!\Big(-\tfrac12(v-c)^\top R(v-c)\Big),
$$
где $R=P+Q$, $c=R^{-1}(Pa+Qb)$.

*Доказательство.* Складываем показатели: $(v-a)^\top P(v-a)+(v-b)^\top Q(v-b)=v^\top Rv-2v^\top(Pa+Qb)+\text{const}$. Применяя тождество $v^\top Rv-2v^\top m=(v-R^{-1}m)^\top R(v-R^{-1}m)-m^\top R^{-1}m$ с $m=Pa+Qb$ (здесь $R^{-1}$ существует, т.к. $R\succ0$), получаем $(v-c)^\top R(v-c)$ плюс не зависящая от $v$ константа. $\blacksquare$

**Лемма B (вариационный принцип Гиббса).** Пусть $\pi_0$ — вероятностная мера, $\kappa>0$, $Z:=\int e^{\kappa f}\,d\pi_0<\infty$. Тогда
$$
\arg\max_{\pi}\Big\{\mathbb E_\pi[f]-\tfrac1\kappa\mathrm{KL}(\pi\Vert\pi_0)\Big\}=\pi^{*},\qquad \frac{d\pi^{*}}{d\pi_0}=\frac{e^{\kappa f}}{Z}.
$$

*Доказательство.* Для $\pi\ll\pi_0$ имеем $\mathrm{KL}(\pi\Vert\pi_0)-\kappa\mathbb E_\pi[f]=\int(\log\tfrac{d\pi}{d\pi_0}-\kappa f)\,d\pi$. По определению $\pi^{*}$ верно $\kappa f=\log\tfrac{d\pi^{*}}{d\pi_0}+\log Z$, поэтому подынтегральное равно $\log\tfrac{d\pi}{d\pi^{*}}-\log Z$, и интеграл даёт $\mathrm{KL}(\pi\Vert\pi^{*})-\log Z$. Минимум по $\pi$ достигается единственно при $\pi=\pi^{*}$. $\blacksquare$

**Лемма C (антимонотонность обращения по Лёвнеру).** Если $0\prec B_0\preceq B_1$, то $B_1^{-1}\preceq B_0^{-1}$.

*Доказательство.* $I=B_0^{-1/2}B_0B_0^{-1/2}\preceq B_0^{-1/2}B_1B_0^{-1/2}=:M$ (конгруэнция сохраняет порядок). У $M$ собственные значения $\ge1$, у $M^{-1}$ — $\le1$, то есть $M^{-1}\preceq I$. Сопрягая обратно: $B_1^{-1}\preceq B_0^{-1}$. $\blacksquare$

> «Чем больше матрица, тем меньше обратная» — этот переход превращает «резкость $A$ растёт» в «ковариация выдачи $C=(\Psi^{-1}+A)^{-1}$ убывает».

**Лемма D (монотонность следа и неотрицательных сумм).** (i) $X\preceq Y\Rightarrow\operatorname{tr}X\le\operatorname{tr}Y$. (ii) $X_k\preceq Y_k,\ \pi_k\ge0\Rightarrow\sum_k\pi_kX_k\preceq\sum_k\pi_kY_k$.

*Доказательство.* (i) $\operatorname{tr}(Y-X)\ge0$, т.к. $Y-X\succeq0$. (ii) $\sum_k\pi_k(Y_k-X_k)\succeq0$ как неотрицательная сумма полуопределённых матриц. $\blacksquare$

---

## 3. Базовый механизм на уровне логов: почему метрики растут, а польза падает

Прежде чем вводить динамику вкусов, покажем, что сам факт обучения на собственных логах уже порождает расхождение метрик. В этом разделе предпочтения **статичны** — единственный источник эффекта — загрязнение наблюдаемого таргета предсказаниями модели.

### 3.1. Загрязнённый клик

Истинную вероятность клика обозначим $\theta(u,v)$ — это латентное предпочтение в мире без рекомендателя. Под влиянием системы пользователь с долей $1-\alpha$ кликает по истинному вкусу, а с долей $\alpha$ — по тому, что показала модель. Примем **модельное предположение** (форма поведения, а не следствие определений): наблюдаемая в логах вероятность клика есть выпуклая смесь
$$
\boxed{\;\eta_t(u,v)=(1-\alpha)\,\theta(u,v)+\alpha\,f_t(u,v),\qquad \alpha=ps.\;}
\tag{3.1}
$$
При $\alpha=0$ петли нет и $\eta_t=\theta$; при $\alpha>0$ наблюдаемое поведение частично порождено самой моделью.

> **Пример.** Пусть $\alpha=0{,}7$. Тогда 70 % сигнала в логе — «кликнул, потому что было на виду», и лишь 30 % — «кликнул, потому что действительно нравится». Если модель ошибочно вывела наверх посредственный объект, пользователь с высокой вероятностью всё равно кликнет — и лог *подтвердит* ошибку.

### 3.2. Переобучение приближает модель к загрязнённому таргету

Модель минимизирует риск на наблюдаемых кликах. При собственной (proper) функции потерь и достаточно богатом классе популяционный оптимум совпадает с условным средним таргета на носителе экспозиции $\operatorname{supp}\mu_t$:
$$
f_{t+1}(x)\approx\eta_t(x)=(1-\alpha)\theta(x)+\alpha f_t(x),\qquad x\in\operatorname{supp}\mu_t.
\tag{3.2}
$$
Оговорка существенна: $\mu_t$ сама задаётся $f_t$, поэтому вне показанных пар модель данных не получает, и (3.2) — приближение на носителе. Содержательно: повторное обучение аппроксимирует не истинный CTR $\theta$, а **загрязнённый** $\eta_t$, в который уже подмешано прошлое предсказание.

### 3.3. Самоподкрепление (Утверждение A)

Для двух объектов $x=(u,i)$, $x'=(u,i')$ обозначим $\Delta_\theta=\theta(x)-\theta(x')$, $\Delta_f=f_t(x)-f_t(x')$, $\Delta_\eta=\eta_t(x)-\eta_t(x')$. Из (3.1):
$$
\boxed{\;\Delta_\eta=(1-\alpha)\,\Delta_\theta+\alpha\,\Delta_f.\;}
\tag{3.3}
$$

**Утверждение A.** Наблюдаемая разница CTR складывается из истинной $(1-\alpha)\Delta_\theta$ и **самоподкрепляющегося** члена $\alpha\Delta_f$, всегда поддерживающего порядок самой модели. Если модель поставила $x$ выше $x'$ ($\Delta_f>0$), то даже при ошибочном ранжировании (истинно $\theta(x)<\theta(x')$, $\Delta_\theta=-D<0$) логи подтвердят порядок модели ($\Delta_\eta>0$) при
$$
\alpha>\frac{D}{D+\Delta_f}.
$$
*Доказательство.* Прямая подстановка в (3.3): $\Delta_\eta=\alpha\Delta_f-(1-\alpha)D>0\Leftrightarrow\alpha\Delta_f>(1-\alpha)D$. $\blacksquare$

### 3.4. Почему растут метрики ранжирования

Популяционные оптимумы AUC, Precision@$L$, NDCG@$L$ сортируют объекты по убыванию **$\eta_t$**, а не $\theta$. Например, для DCG ранжирования $r$, оцениваемого на логах старой модели $f_t$:
$$
\mathbb E\big[\operatorname{DCG}@L^{\mathrm{obs}}(r,u)\big]=(1-\alpha)\sum_{a=1}^{L}d_a\theta(u,\pi_a^r)+\alpha\sum_{a=1}^{L}d_a f_t(u,\pi_a^r),
$$
где $d_a=1/\log_2(a+1)$ — позиционные дисконты. Самоподкрепляющийся член велик, *когда оцениваемое ранжирование $r$ коррелирует с $f_t$* (в частности, при оценке на собственной выдаче модели). Поэтому рост $\operatorname{AUC}^{\mathrm{obs}}$, $\operatorname{NDCG}^{\mathrm{obs}}$, $\operatorname{CTR}^{\mathrm{obs}}$ означает приближение к оптимуму **относительно $\eta_t$**, а не относительно $\theta$; истинные метрики при этом могут падать. (Подчеркнём: рост AUC корректно утверждать лишь при монотонной близости $f_{t+1}$ к $\eta_t$, так как AUC зависит от порядка, а не от калибровки.)

### 3.5. Почему падают Coverage и Diversity

Тот же механизм концентрации обедняет выдачу. Оформим это как *феноменологическое допущение* о динамике экспозиции.

**Предположение (P-exp).** Доля экспозиции $\varrho_t(i)$ эволюционирует мультипликативно: $\varrho_{t+1}(i)\propto\varrho_t(i)\,\varphi(c_t(i))$, где $c_t(i)$ — наблюдаемый engagement, $\varphi$ строго возрастает («богатые богатеют»).

Тогда для двух объектов отношение экспозиций $\varrho_{t+1}(i)/\varrho_{t+1}(j)=[\varrho_t(i)/\varrho_t(j)]\cdot[\varphi(c_t(i))/\varphi(c_t(j))]$ растёт при $c_t(i)>c_t(j)$: популярное становится популярнее. Эффективные Catalog Coverage и intra-list Diversity убывают при концентрации экспозиции на узком подмножестве (в одном из реальных режимов: конечная выборка показов, порог покрытия, детерминированный top-$L$).

**Итог §3.** Скрытая петля заставляет модель аппроксимировать загрязнённый таргет $\eta_t$. Главная формула — (3.3): наблюдаемая разница CTR содержит самоподкрепляющийся член $\alpha\Delta_f$. Поэтому метрики ранжирования могут расти, отражая приближение к оптимуму **относительно собственных логов**, а метрики разнообразия — падать. **Рост наблюдаемых метрик не равнозначен росту истинной пользы.**

---

## 4. Динамическая модель петли

### 4.1. Сквозной пример и латентное пространство

> **Пример (стриминговая платформа).** Платформа рекомендует музыкальные треки. Вкус пользователя — вектор $u\in\mathbb R^d$; для наглядности $d=2$ с осями «джаз» и «рок». Каждый трек — вектор $v$. Аудитория делится на $K=3$ сегмента: любители джаза с центром $\mu_1$, поклонники рока $\mu_2$, слушатели классики $\mu_3$. Близость вкуса и трека — скалярное произведение $\langle u,v\rangle$.

Истинная вероятность отклика: $\theta(u,v)=\sigma(\langle u,v\rangle/\tau)$. Модель с параметрами $w^t$ предсказывает $f_t(u,v)$ и показывает top-$L$ айтемов из конечного каталога $\{v_j\}$ с наибольшим score.

### 4.2. Один шаг петли: процесс и три оператора

> **Один шаг петли (содержательно).**
> 1. **Показ.** Модель ранжирует каталог по $f_t$ и показывает top-$L$. Джаз-любителю чаще выпадает джаз.
> 2. **Загрязнённый клик.** Пользователь кликает с вероятностью, смешивающей истинный вкус $\theta$ и «удобство» рекомендации $f_t$.
> 3. **Дрейф вкуса (β).** Прослушанный трек сдвигает вкус к себе: $u^{t+1}=(1-\beta)u^t+\beta v$.
> 4. **Логирование.** Пара (пользователь, клик) попадает в обучающую выборку.
> 5. **Переобучение (раз в $T_{\mathrm{ret}}$ шагов, только в режиме петли).** Модель подстраивается под смещённые логи и делает выдачу резче.
> 6. **Повтор** — теперь выдача уже концентрированнее.

Формально шаг $s_t=(\{u_i^t\},w^t)\to s_{t+1}$ есть композиция трёх операторов $\mathcal T\circ\mathcal C\circ\mathcal R$:

- **(R) Рекомендация:** $S_i^t=\operatorname{top\text{-}}L_{v_j}\hat r_{w^t}(u_i^t,v_j)$ — каждому пользователю показывают $L$ айтемов с наибольшим score (единственное место, где $w^t$ влияет на наблюдаемое).
- **(C) Отклик:** активный (доля $\rho$) пользователь кликает по $j\in S_i^t$ с вероятностью $s\,\hat r_{w^t}(u_i^t,v_j)+(1-s)\theta(u_i^t,v_j)$; после клика по айтему $v_{j(i,t)}$ его вкус дрейфует (см. §4.4).
- **(T) Переобучение:** каждые $T_{\mathrm{ret}}$ шагов $w^{t+1}=\mathcal T(w^t,\mathcal L^t)$ по накопленному логу; активен только в режиме петли (иначе $w^{t+1}=w^t$). В логи попадает доля $p$ предсказаний, так что совокупная сила петли $\alpha=ps$ согласована с (3.1).

> **Согласование $s$ и $\alpha=ps$.** В операторе клика стоит вес $s$, а не $\alpha$, и это не противоречит §3. Параметр $s$ определяет *поведение* активного пользователя; $p$ — *логирование* (какая доля предсказаний возвращается в обучение). Эффективное загрязнение логов есть $\alpha=ps$; в симуляции $p$ отдельно не моделируется (эквивалентно $p=1$, $\alpha=s$), но в общей модели мы держим оба раздельно.

### 4.3. Пять управляющих параметров

| Параметр | Что описывает | Роль |
|----------|---------------|------|
| $\beta$ | сила дрейфа вкуса за один клик | геометрия движения предпочтений |
| $\rho$ | доля активных за шаг | как часто срабатывает дрейф |
| $s$ | adherence: следование рекомендации | насколько клик определяется показом, а не вкусом |
| $p$ | usage: доля предсказаний в логах | насколько лог загрязнён эндогенным сигналом |
| $\alpha=ps$ | совокупная сила петли | загрязнение логов в целом |

Принципиально: $(\beta,\rho)$ управляют *движением вкусов* (β-канал), $(p,s)$ — *сигналом переобучения* (петлевой канал). Это разные механизмы, и их разделение — стержень анализа.

### 4.4. β-дрейф

$$
\boxed{\;u_i^{t+1}=(1-\beta)u_i^t+\beta v_{j(i,t)}.\;}
\tag{4.1}
$$
Выпуклая комбинация старого вкуса и потреблённого контента.

> **Ключевое наблюдение.** Если двум пользователям показали один и тот же $v$, то $u_i^{t+1}-u_k^{t+1}=(1-\beta)(u_i^t-u_k^t)$: расстояние умножается на $(1-\beta)<1$ за совместный клик. Повторяясь, это геометрически стягивает аудиторию — и именно это, а не переобучение, объясняет наблюдаемый коллапс разнообразия (§7).

### 4.5. Кластеры, разнообразие, разделимость

Мы не предполагаем никакой конкретной вероятностной модели аудитории. **Кластер** — это элемент заданного извне разбиения $\mathcal U=C_1\sqcup\dots\sqcup C_K$ (сегмент вкуса, жанр, демография). Качество разбиения описывают два выборочных числа: центроид $\bar x_k^t=\frac1{n_k}\sum_{i\in C_k}u_i^t$, ковариация $\Sigma_k^t$, разброс $D_k(t)=\operatorname{tr}\Sigma_k^t$, и суммарное разнообразие $D(t)=\sum_k\pi_k D_k(t)$. Разбиение **разделимо**, если центры разнесены сильнее внутреннего разброса:
$$
\Delta_{\min}>c\,\sigma_{\mathrm{in}},\qquad c>0.
\tag{4.2}
$$
Это единственное требование к «кластерности»: ни плотность, ни форма распределения внутри группы в теоремы не входят. Смесь гауссиан — лишь один пример разделимого разбиения.

### 4.6. Эхо-камера и условие совпадения выдачи

**Определение ($(\varepsilon,\phi)$-эхо-камера).** Кластер $C_k$ в режиме $(\varepsilon,\phi)$-эхо-камеры на шаге $t$, если существует общий набор $\mathcal S_k$, $\lvert\mathcal S_k\rvert=L$, такой что доля членов, чья выдача совпадает с $\mathcal S_k$ не менее чем на $L-\varepsilon$ позиций, не ниже $1-\phi$. Идеальная эхо-камера: $\varepsilon=\phi=0$ (все видят один top-$L$).

Малое $D_k(t)$ само по себе не гарантирует совпадение top-$L$: при почти равных score близкие $u_i$ могут давать разные списки. Нужны два условия на скоринг:

> **(EC1) Липшицевость:** $\lvert\hat r_w(u,v)-\hat r_w(u',v)\rvert\le\mathrm{Lip}\cdot\lVert u-u'\rVert$.
> **(EC2) Положительный зазор ранжирования:** для типичного $u$ разрыв между $L$-м и $(L{+}1)$-м score не меньше $m>0$.

**Лемма о пороге (margin).** При (EC1)–(EC2): если $\lVert u_i^t-u_{i'}^t\rVert<m/(2\,\mathrm{Lip})$, то $u_i^t$ и $u_{i'}^t$ дают *одинаковый* top-$L$.

*Доказательство.* При сдвиге $u$ на $\delta$ каждый score меняется не более чем на $\mathrm{Lip}\cdot\lVert\delta\rVert$ (EC1); относительный порядок $L$-го и $(L{+}1)$-го объектов сохраняется, пока $2\,\mathrm{Lip}\cdot\lVert\delta\rVert<m$ (EC2). Тогда top-$L$ не меняется. $\blacksquare$

Связь с §5: убывание $D_k(t)$ уменьшает попарные расстояния $\sim\sqrt{D_k(t)}$; как только они падают ниже $m/(2\,\mathrm{Lip})$, кластер входит в эхо-камеру. Без (EC2) переход не гарантирован — честная граница применимости.

### 4.7. Когда петля включается: чувствительность $J_k$

Включается ли петлевой канал, определяется тем, *различает ли модель пользователей*. Формально — кластерный градиент $J_k=\frac1{n_k}\sum_i\nabla_u\hat r_w\big\rvert_{u_i^t}$. Для *ранжирования* существенна чувствительность *разностей* score $\nabla_u(\hat r_w(u,v_a)-\hat r_w(u,v_b))$ (именно она меняет top-$L$); усреднённый $J_k$ может занулиться из-за компенсации, поэтому $\lVert J_k\rVert$ — наглядный индикатор, а строгим условием служит невырожденность якобиана разностей.

Принципиальное наблюдение разделяет два режима. У случайно инициализированного MLP-скорера оценка почти не зависит от $u$: top-$L$ общий для всех, $\lVert J_k\rVert\approx0$ — модель не различает пользователей, и петлевой канал не может «ухватиться» за индивидуальную структуру. У билинейной модели $\hat r=\sigma(\langle u,v\rangle)$ или у предобученного (warm-start) MLP оценка существенно зависит от $u$, $\lVert J_k\rVert\neq0$. Именно отличие $J_k$ от нуля — ось включения петли (подтверждено экспериментом, §7.5).

### 4.8. Язык сжимающих отображений: неизбежность эхо-камеры

Один шаг β-дрейфа при фиксированной концентрированной выдаче, $u_i\mapsto(1-\beta)u_i+\beta\bar v_k$, есть аффинное **сжимающее отображение** с коэффициентом $1-\beta<1$ и неподвижной точкой $\bar v_k$. По теореме Банаха итерации сходятся к $\bar v_k$ геометрически. Содержательно: как только кластер попал в эхо-камеру, его динамика необратимо стягивает — расстояния множатся на $1-\beta$ и не могут самопроизвольно вырасти. «Поглощаемость» эхо-камеры — *следствие* сжатия, а не постулат.

> **Честная оговорка о круговости.** Это утверждение *условное*: §5 опирается на то, что выдача *уже* концентрирована. Мы доказываем устойчивость/необратимость эхо-камеры, а не её возникновение из произвольного рассеянного старта (скорость входа зависит от того, как быстро top-$L$ становится общим).

### 4.9. Приход и уход пользователей

Реальная аудитория не замкнута. В модели за шаг удаляется доля $r$ наименее активных пользователей, и столько же приходит из *текущего* (пересчитанного) распределения групп. Новички наследуют текущий второй момент $M_k(t)$, поэтому рекурсия (§5) принимает вид $\mathbb E[M_k(t+1)\mid\mathcal F_t]\le q_rM_k(t)+(1-r)\rho\beta^2\sigma_\xi^2$ с $q_r=1-(1-r)(1-q)<1$: замена лишь *замедляет* коллапс, но не останавливает его и не добавляет режим-специфичного эффекта. (Случай *открытого* притока — новички из исходного несжатого распределения — обсуждается в §11.)

---

## 5. Теорема 1: β-канал стягивает разнообразие

### 5.1. Интуиция

Облако точек — вкусы людей одной группы. Всем показывают примерно одно и то же, каждый клик тянет вкус к показанному. β-обновление $u_i\mapsto(1-\beta)u_i+\beta\bar v_k$ — аффинное стягивание к $\bar v_k$. Так как все тянутся к *одной* точке, их взаимное расположение сжимается: общий сдвиг сокращается при вычитании, остаётся множитель $(1-\beta)$. Убывает именно различие между пользователями, а оно и есть разнообразие. Механизм работает по β-каналу и **не требует переобучения** — поэтому коллапс универсален по режиму.

### 5.2. Предположения и две меры разброса

**(A1)** $\beta\in(0,1)$ фиксирована; $\mathcal F_t$ — история. **(A2)** показанный вектор $v_{j(i,t)}=\bar v_k+\xi_i^t$ с условной несмещённостью $\mathbb E[\xi_i^t\mid\mathcal F_t]=0$, $\mathbb E[\lVert\xi_i^t\rVert^2\mid\mathcal F_t]\le\sigma_\xi^2$, мишень $\bar v_k$ фиксирована. **(A3)** активность $\rho$ независимо. **(A4)** $C_k$ фиксирован на горизонте (приток — §4.9).

Помимо разнообразия $D_k(t)$, введём средний квадрат отклонения от мишени $M_k(t)=\frac1{n_k}\sum_i\lVert u_i^t-\bar v_k\rVert^2$. Они связаны разложением «within + between»:
$$
M_k(t)=D_k(t)+\lVert\bar x_k^t-\bar v_k\rVert^2\ \ge\ D_k(t).
\tag{5.1}
$$

> **Почему анализ ведётся на $M_k$, а не на $D_k$.** Рекурсия на $D_k$ при асинхронной активности *не замыкается*. Контрпример: два пользователя, $u_1^0=u_2^0=0$, мишень $\bar v_k\neq0$, без шума; $D_k(0)=0$. Если активен один (вероятность $2\rho(1-\rho)>0$), то $u_1^1=\beta\bar v_k$, $u_2^1=0$, $D_k(1)=\tfrac{\beta^2}4\lVert\bar v_k\rVert^2>0$ — наивная рекурсия «$D_k(t+1)\le qD_k(t)+\text{шум}$» ложна. А $M_k$ складывается из *независимых* индивидуальных слагаемых и замыкается точно; затем $D_k\le M_k$.

### 5.3. Лемма 1.1 (сжатие индивидуального отклонения)

Пусть $\delta_i^t:=u_i^t-\bar v_k$. **При (A1)–(A3) для каждого $i$:**
$$
\mathbb E[\lVert\delta_i^{t+1}\rVert^2\mid\mathcal F_t]\le q\lVert\delta_i^t\rVert^2+\rho\beta^2\sigma_\xi^2,\qquad q=1-\beta\rho(2-\beta)<1.
$$

*Доказательство.* Активный: $\delta_i^{t+1}=(1-\beta)\delta_i^t+\beta\xi_i^t$; неактивный: $\delta_i^{t+1}=\delta_i^t$. Возводя в квадрат и беря $\mathbb E[\cdot\mid\mathcal F_t]$, перекрёстный член $2\beta(1-\beta)\langle\delta_i^t,\mathbb E[\xi_i^t\mid\mathcal F_t]\rangle=0$ по (A2). Коэффициент при $\lVert\delta_i^t\rVert^2$: $\rho(1-\beta)^2+(1-\rho)=1-\beta\rho(2-\beta)=q$. $\blacksquare$

> Условная несмещённость (A2) существенна: в реальной системе $\xi_i^t$ зависит от $u_i^t$ (показанный айтем выбирается по вектору пользователя), поэтому безусловного $\mathbb E\xi=0$ недостаточно — явная граница применимости.

### 5.4. Лемма 1.2 (рекурсия на $M_k$)

**При (A1)–(A4):** $\mathbb E[M_k(t+1)\mid\mathcal F_t]\le qM_k(t)+\rho\beta^2\sigma_\xi^2$.

*Доказательство.* $M_k(t)=\frac1{n_k}\sum_i\lVert\delta_i^t\rVert^2$; применяем Лемму 1.1 к каждому $i$ и усредняем. $\blacksquare$

### 5.5. Теорема 1 и следствия

**Теорема 1 (β-сжатие разнообразия).** При (A1)–(A4)
$$
\boxed{\;\mathbb E\,D_k(t)\le M_k(0)\,q^t+\frac{\beta\sigma_\xi^2}{2-\beta},\qquad q=1-\beta\rho(2-\beta)<1.\;}
$$

*Доказательство.* Безусловное матожидание Леммы 1.2 даёт числовую рекурсию $a_{t+1}\le qa_t+c$, $a_t=\mathbb E M_k(t)$, $c=\rho\beta^2\sigma_\xi^2$, с решением $a_t\le q^t a_0+\frac{c}{1-q}$; пол $\frac{c}{1-q}=\frac{\rho\beta^2\sigma_\xi^2}{\beta\rho(2-\beta)}=\frac{\beta\sigma_\xi^2}{2-\beta}$. По (5.1) $\mathbb E D_k(t)\le\mathbb E M_k(t)$. $\blacksquare$

> **О сходимости.** Доказано утверждение про *математическое ожидание*; отсюда оценка с высокой вероятностью по неравенству Маркова $\Pr(D_k(t)>a)\le\mathbb E D_k(t)/a$. Сходимость почти наверное к окрестности пола следует из того, что $M_k(t)$ — неотрицательный почти-супермартингал с суммируемым возмущением (теорема Роббинса–Сигмунда).

**Следствие 1.1 (нет коллапса при $\beta=0$).** При $\beta=0$: $q=1$, пол $=0$, $\mathbb E M_k(t)\le M_k(0)$ — убывания нет.

**Следствие 1.2 (универсальность по режиму).** $q$ зависит лишь от $\beta,\rho$ и (через $\sigma_\xi$) от концентрации выдачи — не от факта переобучения. Поэтому режимы с переобучением, без него и с честным переобучением дают один закон убывания; режим без следования рекомендациям ($s=0$, большое $\sigma_\xi$) коллапсирует слабее.

**Следствие 1.3 (время до эхо-камеры).** $\mathbb E M_k(t)\le\varepsilon_0$ за $T_{\mathrm{collapse}}\le\dfrac{\ln(M_k(0)/(\varepsilon_0-\text{пол}))}{\ln(1/q)}=O\!\big(\tfrac{\log(1/\varepsilon_0)}{\log(1/q)}\big)$ шагов; вместе с Леммой о пороге (§4.6) это и есть строгая версия «неизбежности эхо-камеры», *условная* на концентрации выдачи (A2) и положительном margin.

> **Связь с петлевым каналом (ключевая для §6).** Шумовой пол $\dfrac{\beta\sigma_\xi^2}{2-\beta}$ зависит от $\sigma_\xi^2=\operatorname{tr}\Sigma_\xi$ — разброса показанного. Чем у́же выдача (меньше $\sigma_\xi^2$), тем ниже пол и тем полнее коллапс. Петлевой канал (§6) действует именно здесь: переобучение сужает выдачу $\Sigma_\xi$ и тем самым опускает пол.

> **Привязка к числам.** При $\beta=0{,}005$, $\rho=0{,}8$: $q\approx0{,}992$. За один шаг горизонта активный пользователь кликает многократно (каждый клик — отдельное сжатие), поэтому эффективное число сжатий $\gg T$, что объясняет наблюдаемое $\sqrt{\operatorname{tr}\hat\Sigma_T/\operatorname{tr}\hat\Sigma_0}\approx\sqrt{0{,}03}\approx0{,}17$.

**Главный вывод §5.** Коллапс суммарного разброса $\operatorname{tr}\hat\Sigma$ — прямое следствие β-дрейфа по концентрированной выдаче, не зависящее от наличия петли переобучения.

---

## 6. Петлевой канал: кластерный reward и сужение выдачи при переобучении

Теорема 1 показала, что коллапс разнообразия универсален по режиму: он одинаков и с переобучением, и без него. Вопрос петлевого канала — вносит ли переобучение *дополнительный, специфический* вклад сверх β-дрейфа. Абляция §7.6 локализует ответ: специфический разрыв $\mathrm{KL}(P_T\Vert P_0)$ между петлёй и заморозкой **целиком приходится на дисперсионную (ковариационную) часть**, а сдвиг центра незначим. Чтобы вскрыть *механизм*, мы вводим кластерный reward как популяционный сигнал логов, описываем переобучение как улучшение политики и показываем, что оно сужает ковариацию выдачи — в петле глубже, чем при замороженной модели, — тем самым опуская шумовой пол β-коллапса (§5.5).

**Кластерный reward.** Зафиксируем кластер $C_k$ и обозначим наблюдаемый отклик пользователя $u\in C_k$ на показанный айтем $v$ бинарной величиной $Y_t\in\{0,1\}$ (клик, просмотр, лайк), $\eta_t(u,v)=\mathbb P(Y_t=1\mid U=u,V=v)$ — наблюдаемая вероятность отклика (§3). *Кластерным reward* айтема $v$ называется средний по кластеру логируемый сигнал
$$
\boxed{\;c_{k,t}(v):=\mathbb E\big[Y_t\mid U\in C_k,\ V=v,\ V\ \text{показан}\big]=\mathbb E_{U\sim P_{k,t}}\big[\eta_t(U,v)\big],\;}
$$
где $P_{k,t}$ — распределение вкусов внутри $C_k$ на шаге $t$. Это аналитическая популяционная величина (система не обязана вычислять её явно), описывающая, насколько айтем $v$ в среднем «успешен» в логах кластера. Именно $c_{k,t}$ — тот сигнал, который переобучение стремится максимизировать выдачей; в условиях петли он эндогенен, поскольку наблюдаемая $\eta_t$ уже содержит прошлое предсказание $f_t$ (3.1).

> **Граница применимости.** Дальнейшее — *специальная модель*, выбранная ради точных формул. Конечный каталог и детерминированный top-$L$ из §4 здесь заменяются непрерывным гауссовым каталогом и *рандомизированной* политикой (экспоненциальный наклон). Это разные операторы (top-$L$ разрывен по марже, наклон гладкий); строгий мост между ними даёт Лемма о пороге §4.6, и здесь мы работаем в гладком приближении.

### 6.1. Гауссов каталог и аппроксимация Лапласа

Для кластера $k$: вкусы $U\mid Z=k\sim\mathcal N(\mu_k,S_{k,t})$, каталог $V\mid Z=k\sim\nu_k=\mathcal N(\mu_k,\Psi_k)$, $\Psi_k\succ0$.

Скор реальной модели — сложная нелинейная функция, но вблизи максимума приближается параболой.

> **Предложение 6.1 (Лаплас).** Пусть $r_w(u,\cdot)\in C^2$ и имеет внутренний максимум в $v^{*}(u)$ (это *допущение*). Тогда $r_w(u,v)=r_w(u,v^{*})-\tfrac12(v-v^{*})^\top A(v-v^{*})+o(\lVert v-v^{*}\rVert^2)$, $A:=-\nabla_v^2 r_w(u,v^{*})\succeq0$.

Матрицу $A$ понимаем как **резкость** (precision) выдачи: большое $A$ — узкий пик (показывается почти одно и то же), малое — пологий профиль (выдача разнообразна).

> **Оговорка о существовании максимума (необходима).** Истинная утилита $\theta=\sigma(\langle u,v\rangle/\tau)$ **не** имеет внутреннего максимума по $v$: $\nabla_v\theta=\tfrac1\tau\sigma'(\cdot)u\ne0$ при $u\ne0$, и вдоль луча $v=cu$, $c\to\infty$, $\theta\to1$. Поэтому квадратичная вовлечённость с максимумом в $\mu_k$ требует одного из: (i) компактной области айтемов; (ii) штрафа за норму $c(u,v)=\theta-\lambda\lVert v\rVert^2$; (iii) локального Лапласа у моды насыщающегося скорера. Квадратичная форма принимается **как модельное допущение** в одном из этих режимов.

### 6.2. Скоринг, политика и ковариация выдачи

Центрированный скор и политика (наклон каталога):
$$
r_{A_{k,t}}(v)=-\tfrac12(v-\mu_k)^\top A_{k,t}(v-\mu_k),\qquad\pi_{k,t}(dv)\propto\exp(r_{A_{k,t}}(v))\,\nu_k(dv).
$$

> **Предложение 6.2 (распределение выдачи).** При $A_{k,t}\succeq0$: $V_t\mid Z=k\sim\mathcal N(\mu_k,C_{k,t})$, $C_{k,t}=(\Psi_k^{-1}+A_{k,t})^{-1}$.
> *Доказательство.* Лемма A с $P=\Psi_k^{-1}$, $Q=A_{k,t}$, $a=b=\mu_k$: $R=\Psi_k^{-1}+A_{k,t}$, $c=\mu_k$, ковариация $C_{k,t}=R^{-1}$. $\blacksquare$

Формула $C=(\Psi^{-1}+A)^{-1}$ — центральная: **разброс показанного $C$ тем меньше, чем больше резкость $A$.** В пределе $A\to\infty$ выдача стягивается в точку ($C\to0$).

> **Тождество $C_{k,t}=\Sigma_\xi^t$.** В центрированной модели мишень $\bar v_k=\mu_k$, показанный $V_t\sim\mathcal N(\mu_k,C_{k,t})$, поэтому отклонение $\xi=V_t-\mu_k$ удовлетворяет $\mathbb E[\xi]=0$ (точно) и $\operatorname{Cov}(\xi)=C_{k,t}$. Значит $\Sigma_\xi^t=C_{k,t}$ — это в точности разброс показанного $\sigma_\xi^2=\operatorname{tr}C_{k,t}$, входящий в шумовой пол Теоремы 1 (§5.5).

### 6.3. Переобучение как улучшение политики и локальная кривизна reward

Один шаг переобучения опишем как **улучшение политики с KL-регуляризацией**: новая выдача максимизирует ожидаемый кластерный reward, не отходя слишком далеко от старой,
$$
\pi_{k,t+1}=\arg\max_\pi\Big\{\mathbb E_{V\sim\pi}\big[c_{k,t}(V)\big]-\tfrac1\kappa\mathrm{KL}(\pi\Vert\pi_{k,t})\Big\}.
$$
По вариационному принципу Гиббса (Лемма B) решение есть экспоненциальное перевзвешивание старой политики, $\pi_{k,t+1}(dv)\propto e^{\kappa c_{k,t}(v)}\pi_{k,t}(dv)$: айтемы с высоким reward получают больший вес.

> **Допущение о форме переобучения (не следствие ERM).** Это зеркальный спуск над политиками, а **не** эмпирическая минимизация риска на логах. При обычном supervised-дообучении кривизна выученного скора может расти, убывать или поворачиваться; поэтому правило роста резкости — **модельная гипотеза** о форме оператора $\mathcal T$, проверяемая эмпирически. Микрообоснование (кликовый reward, BCE-потеря) выносится в §6.7.

**Локальная кривизна reward.** Кластерный reward $c_{k,t}$ *не обязан быть квадратичным*: он может задаваться нейросетевым скором, кликовой моделью или эмпирическим CTR. Для вывода нужна лишь его локальная форма в области фактической выдачи. Разложим $c_{k,t}$ по Тейлору около центра выдачи $m_{k,t}$ (в центрированной модели $m_{k,t}=\mu_k$):
$$
c_{k,t}(v)\approx c_{k,t}(m_{k,t})+g_{k,t}^{\top}(v-m_{k,t})-\tfrac12(v-m_{k,t})^\top H_{k,t}(v-m_{k,t}),\qquad H_{k,t}:=-\nabla_v^2 c_{k,t}(m_{k,t}).
$$
Градиент $g_{k,t}$ сдвигает *центр* выдачи, кривизна $H_{k,t}$ меняет её *ковариацию*. Если reward локально вогнут в области выдачи (имеет там максимум — типично для тематически связного кластера, §6.8), то $H_{k,t}\succeq0$. Квадратичная форма $\widehat c_{k,t}(v)=c^0-\tfrac12(v-\mu_k)^\top H_{k,t}(v-\mu_k)$ — это и есть локальное Тейлор-приближение, а не отдельное допущение о природе reward.

Подставляя локальную форму в перевзвешивание и складывая показатели (Лемма A, точности складываются, центр сохраняется), получаем рекурсию резкости
$$
\boxed{\;A_{k,t+1}=A_{k,t}+\kappa H_{k,t}\succeq A_{k,t},\qquad\text{равносильно}\qquad C_{k,t+1}^{-1}=C_{k,t}^{-1}+\kappa H_{k,t}.\;}
\tag{6.1}
$$
Каждое переобучение прибавляет к резкости неотрицательную добавку — резкость только растёт, ковариация выдачи $C=(\Psi^{-1}+A)^{-1}$ только сужается (в локальном приближении).

### 6.4. Теорема 2: петля сужает выдачу глубже, чем заморозка

**Сужение вдоль траектории.** Из (6.1) и Леммы C: $\Psi_k^{-1}+A_{k,t+1}\succeq\Psi_k^{-1}+A_{k,t}\succ0\Rightarrow C_{k,t+1}\preceq C_{k,t}$ (строго по направлению $z$ с $z^\top H_{k,t}z>0$): каждое переобучение сужает выдачу относительно собственного прошлого.

**Теорема 2 (петля vs заморозка в гауссовой модели).** Пусть старты совпадают $A_{k,0}^{\mathrm{closed}}=A_{k,0}^{\mathrm{stat}}=A_{k,0}$; заморозка фиксирует $A_{k,t}^{\mathrm{stat}}\equiv A_{k,0}$, петля переобучается с $H_{k,t}^{\mathrm{closed}}\succeq0$. Тогда при всех $t$
$$
A_{k,t}^{\mathrm{closed}}=A_{k,0}+\kappa\!\sum_{s<t}H_{k,s}^{\mathrm{closed}}\ \succeq\ A_{k,0}=A_{k,t}^{\mathrm{stat}}
\quad\overset{\text{Л.C}}{\Longrightarrow}\quad
\boxed{\;C_{k,t}^{\mathrm{closed}}\preceq C_{k,t}^{\mathrm{stat}}.\;}
$$

*Доказательство.* Накопленная добавка $A_{k,t}^{\mathrm{closed}}-A_{k,0}=\kappa\sum_{s<t}H_{k,s}^{\mathrm{closed}}\succeq0$ как сумма полуопределённых матриц. Прибавление к $\Psi_k^{-1}$ сохраняет $\succ0$ и Loewner-порядок, а Лемма C переворачивает порядок при обращении: $C_{k,t}^{\mathrm{closed}}=(\Psi_k^{-1}+A_{k,t}^{\mathrm{closed}})^{-1}\preceq(\Psi_k^{-1}+A_{k,0})^{-1}=C_{k,t}^{\mathrm{stat}}$. $\blacksquare$

**Связь с коллапсом вкусов (почему это углубляет деградацию).** Второй момент вкусов вокруг фиксированной мишени кластера подчиняется матричной рекурсии
$$
S_{k,t+1}=q\,S_{k,t}+\rho\beta^2\,C_{k,t},\qquad q=1-\beta\rho(2-\beta),
$$
операторному аналогу Леммы 1.1 (рекурсия замыкается именно для второго момента вокруг *фиксированной* мишени, а не для выборочной ковариации — см. контрпример §5.2; свободный член — это $\Sigma_\xi^t=C_{k,t}$ в центрированной модели). При $C_{k,t}\to C_k^\infty$ стационарный момент $S_k^\infty=\tfrac{\beta}{2-\beta}C_k^\infty$ пропорционален ковариации выдачи. Поэтому $C^{\mathrm{closed}}\preceq C^{\mathrm{stat}}$ влечёт $S^{\infty,\mathrm{closed}}\preceq S^{\infty,\mathrm{stat}}$: **петля опускает пол β-коллапса, и вкусы сжимаются глубже, чем при замороженной модели**. Поскольку $C$ сужается как матрица (не только по следу), сжатие анизотропно — след петли виден именно в *форме* ковариации вкусов (дисперсионная часть $\mathrm{KL}$, §6.9), а не в сдвиге центров.

> **Оговорка: центрированная модель не активирует персонализированную петлю.** Скор $r_{A_{k,t}}(v)$ не зависит от $u$ ($\nabla_u r=0$): модель не различает пользователей. Это *сужение каталожного уровня* (всем — одна форма выдачи). Истинный петлевой механизм требует $\lVert J_k\rVert\neq0$ — это персонализированная модель §6.6.

### 6.5. Самоподкрепление и его ограниченность

Пусть $\widehat c_{k,t}=(1-\alpha)c_k^{\mathrm{true}}+\alpha r_{A_{k,t}}$ (вовлечённость частично порождена прошлой выдачей — это и есть загрязнение логов §3). Тогда $H_{k,t}=(1-\alpha)H_k^{\mathrm{true}}+\alpha A_{k,t}$ и
$$
A_{k,t+1}=(1+\kappa\alpha)A_{k,t}+\kappa(1-\alpha)H_k^{\mathrm{true}}.
$$
Это аффинная рекурсия $A_{t+1}=\lambda A_t+cH$, $\lambda=1+\kappa\alpha$, $c=\kappa(1-\alpha)$, с замкнутым решением (проверяется подстановкой, коммутативность не нужна):
$$
A_{k,t}=\lambda^t A_{k,0}+c\,\frac{\lambda^t-1}{\lambda-1}H.
$$
При $\alpha>0$ ($\lambda>1$) резкость растёт **геометрически** со скоростью $1+\kappa\alpha$: чем сильнее загрязнение $\alpha$, тем быстрее заострение. Это самоподкрепляющийся член $\kappa\alpha A_{k,t}$.

> **Ограниченность (честная оговорка).** Неограниченный рост $A_{k,t}\to\infty$ даёт $C_{k,t}\to0$ — *полный* коллапс, что сильнее эмпирической картины (пол $\operatorname{tr}\hat\Sigma\sim0{,}8$, не $0$). Ограниченность в реальности обеспечивается конечным горизонтом, неявной регуляризацией ERM или конечным каталогом. Минимальный способ закодировать пол — член забывания $A_{k,t+1}=(1-\lambda_{\mathrm{reg}})A_{k,t}+\kappa H_{k,t}$, который при $\kappa\alpha<\lambda_{\mathrm{reg}}$ сходится к конечной точке $A^{*}=\tfrac{\kappa(1-\alpha)}{\lambda_{\mathrm{reg}}-\kappa\alpha}H_k^{\mathrm{true}}$ и положительному полу $C^{*}\succ0$. Нерегуляризованную форму мы сохраняем для *направления* эффекта (знак), но не для величины.

### 6.6. Персонализированный порог сжатия (условие активации)

При персонализированной политике $\pi_A(v\mid u,k)\propto\nu_k(v)\exp(-\tfrac12(v-u)^\top A(v-u))$ (по Лемме A):
$$
V\mid U=u\sim\mathcal N(m_A(u),C_A),\quad C_A=(\Psi_k^{-1}+A)^{-1},\quad m_A(u)=\mu_k+B_A(u-\mu_k),\quad B_A=C_AA,
$$
$$
\Sigma_V(A)=\underbrace{C_A}_{\downarrow\text{ с резкостью}}+\underbrace{B_AS_kB_A^\top}_{\text{может }\uparrow}.
$$
Здесь $\nabla_u m_A(u)=B_A\ne0$ — модель *различает* пользователей ($\lVert J_k\rVert\neq0$): это режим, где петля активируется. **Три уровня строгости порога:**

1. **изотропный** ($\Psi_k=\psi I$, $S_k=\sigma_u^2 I$, $A=aI$): полная дисперсия показанного убывает с резкостью $\Leftrightarrow\ \boxed{\sigma_u^2<\dfrac{1+a\psi}{2a}}$ (строго, точно);
2. **коммутирующий диагональный:** покоординатно $2a_\ell s_\ell<1+a_\ell\psi_\ell$;
3. **общий матричный:** отображение $A\mapsto C_A+B_AS_kB_A^\top$ **не** Loewner-монотонно (контрпример ниже), гарантируется лишь сжатие следа.

*Доказательство (1).* $C_A=\tfrac{\psi}{D}$, $B_A=\tfrac{a\psi}{D}$, $D=1+a\psi$; $\sigma_V^2(a)=\tfrac{\psi}{D}+(\tfrac{a\psi}{D})^2\sigma_u^2$. Дифференцируя (с $dD/da=\psi$, $D-a\psi=1$): $\tfrac{d\sigma_V^2}{da}=\tfrac{\psi^2}{D^3}[2a\sigma_u^2-(1+a\psi)]$, множитель $\tfrac{\psi^2}{D^3}>0$, откуда $\tfrac{d\sigma_V^2}{da}<0\Leftrightarrow\sigma_u^2<\tfrac{1+a\psi}{2a}$. $\blacksquare$

*Контрпример к (3).* Существуют $A_1\succeq A_0$ с $\Sigma_V(A_1)\not\preceq\Sigma_V(A_0)$ (разность имеет отрицательные собственные значения): рост $C_A^{-1}$ уменьшает шум $C_A$, но усиливает $B_A=C_AA\to I$, и член $B_AS_kB_A^\top$ растёт в других направлениях. (Проверено численно.) Поэтому в общем случае — лишь следовые достаточные условия, не скалярный порог.

Содержательно порог означает: петля сужает показанное только для *достаточно сжатых* кластеров ($\sigma_u^2$ мало). β-дрейф (§5) доводит кластер до порога, после чего переобучение дополнительно заостряет выдачу.

### 6.7. Микрообоснование кривизны: кликовый reward и BCE-потеря

Условие $H_{k,t}\succeq0$ из §6.3 — не произвольное допущение: оно естественно возникает, когда reward порождается обучением скор-модели на кликах. Покажем это для бинарной кросс-энтропии (BCE) — стандартной функции потерь рекомендателя.

Пусть модель имеет скор $s_w(u,v)\in\mathbb R$, а вероятность отклика предсказывается как $\widehat p_w(u,v)=\sigma(s_w(u,v))$. Скор может быть двухбашенным $s_w(u,v)=\tfrac1{\tau_s}\langle\phi_w(u),\psi_w(v)\rangle+b_v$ или метрическим $s_w(u,v)=-\tfrac1{2T}\lVert\phi_w(u)-\psi_w(v)\rVert^2+b_v$. Для наблюдаемого таргета $Y\in\{0,1\}$ потеря $\ell_{\mathrm{BCE}}(Y,s)=-Y\log\sigma(s)-(1-Y)\log(1-\sigma(s))=\log(1+e^s)-Ys$; в популяционной форме с $\eta_t(u,v)=\mathbb E[Y_t\mid u,v]$ ожидаемая потеря равна $\log(1+e^{s})-\eta_t s$. Кластерный reward берём как отрицательную кластерную BCE-потерю
$$
c_{k,t}^{\mathrm{BCE}}(v)=-\,\mathbb E_{U\sim P_{k,t}}\big[\ell_{\mathrm{BCE}}\big(\eta_t(U,v),\,s_w(U,v)\big)\big],
$$
то есть айтем получает высокий reward, когда модель хорошо объясняет наблюдаемые клики кластера на нём.

> **Два разных reward и два разных объекта кривизны (честная оговорка).** Reward вовлечённости (6.1) — «айтемы, которые пользователь кликает» — и reward $c^{\mathrm{BCE}}$ — «айтемы, которые модель точно предсказывает» — это *разные* сигналы; их отождествление не предполагается. Мы лишь показываем, что *каждый* из них даёт неотрицательную кривизну $H\succeq0$ при условиях ниже. Кроме того, концентрацию выдачи top-$L$ напрямую определяет кривизна *скора* $-\nabla_v^2 s_w$, тогда как гессиан BCE $\nabla_v^2\ell$ — другая матрица; отождествление кривизны улучшения политики с гессианом BCE — модельный выбор того же статуса, что и KL-форма §6.3.

**Локальная кривизна BCE.** Зафиксируем $u\in C_k$, обозначим $z(v)=s_w(u,v)$, $\eta(v)=\eta_t(u,v)$. В стандартном популяционном supervised-обучении при дифференцировании по $v$ таргет $\eta$ трактуется как фиксированная метка из логов (приближение: $\eta(u,v)$ зависит от $v$, и члены $\nabla_v\eta$ отбрасываются). Тогда
$$
\nabla_v\ell=(\sigma(z)-\eta)\,\nabla_v z,\qquad
\nabla_v^2\ell=\sigma(z)(1-\sigma(z))\,\nabla_v z\,\nabla_v z^\top+(\sigma(z)-\eta)\,\nabla_v^2 z,
$$
и так как $c^{\mathrm{BCE}}=-\ell$, кластерная кривизна reward равна $H_{k,t}^{\mathrm{BCE}}=\mathbb E_{U\sim P_{k,t}}[\nabla_v^2\ell]\big\rvert_{v=m_{k,t}}$. Знак $H_{k,t}^{\mathrm{BCE}}$ определяют два слагаемых:

1. **Скор локально линеен по айтему** ($\nabla_v^2 z=0$): тогда $H_{k,t}^{\mathrm{BCE}}=\mathbb E_{U\sim P_{k,t}}[\sigma(z_U)(1-\sigma(z_U))\,\nabla_v z_U\,\nabla_v z_U^\top]\succeq0$ как матожидание неотрицательно определённых матриц.
2. **Модель локально откалибрована** ($\sigma(z_U)\approx\eta_U$): второй член $(\sigma-\eta)\nabla_v^2 z$ мал, и снова $H_{k,t}^{\mathrm{BCE}}\succeq0$.
3. **Общий нелинейный скор:** член $(\sigma-\eta)\nabla_v^2 z$ знаконеопределён, поэтому $H\succeq0$ не следует автоматически; это *проверяемое локальное условие* — отрицательная BCE-потеря локально вогнута по направлениям айтема в области выдачи.

**Вывод §6.7.** Квадратичная форма $\widehat c$ с $H\succeq0$ — локальное Тейлор-приближение кликового (BCE) сигнала, а не исходная гипотеза о форме reward; матрица $H$ соответствует локальной кривизне BCE-потери. Именно при $H\succeq0$ переобучение сужает ковариацию выдачи (§6.3–6.4).

### 6.8. Когда кластерный reward возникает в данных

Кластерный reward — не искусственная конструкция: он естественно появляется там, где пользователи внутри группы дают похожий сигнал отклика. Перечислим основные источники. **(1) Явные сегменты** (новизна, активность, регион, жанровые или демографические группы): оценивается сегментный CTR $\widehat c_{k,t}(v)=\#\{\text{клики по }v\text{ в }C_k\}/\#\{\text{показы }v\text{ в }C_k\}$, в пределе $=c_{k,t}(v)$. **(2) Неявные кластеры в латентном пространстве**: даже без явной сегментации группы близких эмбеддингов $u_i^t\in C_k$ задают $c_{k,t}$ естественно — это типично для двухбашенных и matrix-factorization моделей. **(3) Тематические группы** (джаз/рок/классика, спорт/политика, бюджет/премиум): reward имеет выраженный максимум в тематически близкой области, что и даёт локальную вогнутость $H_{k,t}\succeq0$ и сужение выдачи. **(4) Батчевое обучение**: при многих пользователях одного сегмента в батче градиент $\mathbb E[(Y-\sigma(s_w(U,V)))\nabla_w s_w(U,V)\mid U\in C_k]$ несёт усреднённый кластерный сигнал — модель использует $c_{k,t}$ неявно. **(5) Смещение экспозиции** (§3): частые показы набора айтемов кластеру повышают их наблюдаемый reward, а переобучение усиливает их показ — петля делает $c_{k,t}$ эндогенным. **(6) Многомодальная аудитория** $P_t=\sum_k\pi_k P_{k,t}$: глобальный reward $\mathbb E_{U\sim P_t}[\eta_t(U,v)]$ сглажен, тогда как кластерный $c_{k,t}$ может иметь резкий максимум — поэтому петлевой эффект диагностируется именно на уровне кластеров ($K\ge2$, §6.6).

### 6.9. Метрика следа петли и итог

Чтобы измерить именно *переформирование разброса* (а не его объём), удобна дисперсионная часть гауссова расхождения двух ковариаций:
$$
\mathcal D(\Sigma_T\Vert\Sigma_0)=\tfrac12\big(\operatorname{tr}(\Sigma_0^{-1}\Sigma_T)-d+\ln\det\Sigma_0-\ln\det\Sigma_T\big)\ge0,
\tag{6.2}
$$
растущая и при уменьшении объёма ($-\ln\det\Sigma_T$), и при росте анизотропии. Полный гауссов $\mathrm{KL}(P_T\Vert P_0)$ распадается на сдвиг среднего $\tfrac12(\mu_0-\mu_T)^\top\Sigma_0^{-1}(\mu_0-\mu_T)$ и дисперсионный член $\mathcal D$. Предсказание гауссовой модели: специфический след петли (более глубокое и анизотропное $C^{\mathrm{closed}}\preceq C^{\mathrm{stat}}$) проявляется именно в *дисперсионном* члене, а не в сдвиге среднего. Это подтверждено абляцией (§7.6): весь разрыв петля−заморозка $+0{,}79$ — в дисперсии, сдвиг среднего $+0{,}002$ незначим.

**Итог §6.** Петлевой канал — это эффект *заострения выдачи переобучением*: кластерный reward задаёт сигнал, локальная вогнутость которого ($H\succeq0$, обоснована через BCE в §6.7) ведёт к росту резкости $A$ (6.1) и сужению ковариации показанного $C=(\Psi^{-1}+A)^{-1}$, в петле глубже, чем при заморозке (Теорема 2). Это опускает шумовой пол β-коллапса (§5.5) и переформирует ковариацию вкусов. Канал активируется лишь при различимости пользователей ($\lVert J_k\rVert\neq0$, §6.6) и при пороге концентрации $\sigma_u^2<(1+a\psi)/(2a)$. В отличие от β-канала, он специфичен для переобучения. Предсказания канала сопоставлены с экспериментами §7; прямой замер сужения выдачи $C$ и его границы обсуждаются в §7.7.

---

## 7. Эксперименты и их результаты

Все предсказания модели проверены серией вычислительных экспериментов на агентной симуляции. Здесь описаны постановка, что именно делалось в каждом эксперименте, и результаты.

### 7.1. Архитектура симуляции

Один шаг $t\to t+1$: формирование top-$K$ рекомендаций → генерация кликов по adherence-модели → запись логов → β-дрейф эмбеддингов → переобучение (раз в $T_{\mathrm{ret}}$, только в режиме петли) → подсчёт метрик. Компоненты:

- **Рекомендатель:** двухбашенный MLP-классификатор (user/item-башни $\mathbb R^d\to\mathbb R^{64}$, поэлементное произведение, ReLU, sigmoid; Adam, BCE) либо матричная факторизация $\hat r=\sigma(u^\top Mv+b)$.
- **Генератор пользователей:** смесь гауссиан; на шаге уходит доля $r$ наименее активных, столько же приходит из текущего пересчитанного GMM (либо эмпирический генератор для реальных данных).
- **Модель кликов:** adherence $s$, активность $\rho$, шум.
- **Четыре режима сравнения:**

| Режим | Поведение |
|-------|-----------|
| `closed_loop` | полная петля: переобучение на собственных логах, $\alpha>0$. |
| `static` | $\alpha>0$, но модель заморожена (не переобучается). |
| `fresh_oracle` | переобучение только на истинных предпочтениях (контрфактический оракул). |
| `no_influence` | $\alpha=0$: клики только по истинным предпочтениям. |

Базовая конфигурация: $d=8$, $N=M=300$, $K=3$, top-$K=10$, $T=100$, $T_{\mathrm{ret}}=10$, замена 5 %/шаг, $\alpha=0{,}7$, $\beta=0{,}005$, межкластерное расстояние $5{,}0$.

### 7.2. Основные эксперименты H1–H6

- **H1 (систематическое усиление дрейфа).** *Что делалось:* 4 режима × 5 seeds, измерялась дивергенция $\mathrm{KL}(P_t\Vert P_0)$. *Результат:* $\mathrm{KL}$ в петле $16{,}88$ против $7{,}31$ в `no_influence` ($\times2{,}3$). Подтверждает рост расхождения. Нюанс: `static`/`fresh_oracle` дают $\mathrm{KL}\approx17$, как и петля, — первый сигнал того, что эффект не от переобучения как такового.
- **H2 (K-модальная структура эхо-камер).** *Что делалось:* свип $K\in\{1,2,4\}$ и межкластерного расстояния; измерялись внутрикластерная дисперсия и силуэт. *Результат:* силуэт не деградирует при $K>1$ (кластеры не сливаются — каждый формирует свою эхо-камеру); внутрикластерная дисперсия падает на $77$–$85\%$, быстрее при большем $K$.
- **H3 (специфичность для петли — исходно).** *Что делалось:* сравнение коллапса $\operatorname{tr}\hat\Sigma$ по режимам. *Результат:* петля $-96{,}9\%$, `no_influence` $-86{,}1\%$. В исходной форме гипотеза *отвергнута* диагностикой §7.6 (эффект универсален при $\beta>0$).
- **H4 (фазовая диаграмма).** *Что делалось:* сетка $8\times8$ по (adherence $\alpha$, активность $\rho$), 3 seeds на точку (192 симуляции); метрика — остаточная доля дисперсии. *Результат:* при $\rho=1$ доля монотонно убывает с $0{,}268$ ($\alpha=0$) до $0{,}018$ ($\alpha=1$). Граница коллапса определяется произведением $\alpha\cdot\rho$.
- **H5 (антиколлапсные интервенции).** *Что делалось:* сравнение пяти политик (top-$K$, ε-greedy с $\varepsilon\in\{0{,}1;0{,}3\}$, softmax, diversity-aware MMR) по сохранению $\operatorname{tr}\hat\Sigma$ и качеству. *Результат:* diversity-aware $0{,}442$ против baseline $0{,}289$ (**+53 %**) при том же качестве; ε-greedy и softmax коллапс *усиливают* (рандомизация тянет к центру). Рандомизация — не защита; явный diversity-контроль — да.
- **H6 (скрытое vs наблюдаемое качество).** *Что делалось:* раздельно фиксировались наблюдаемое (по кликам) и истинное (по $\theta$) NDCG-подобное качество во времени. *Результат:* наблюдаемое $0{,}754\to0{,}686$ ($-9\%$), истинное $0{,}690\to0{,}662$ ($-4{,}1\%$); наблюдаемое **держится выше** истинного на всём горизонте — прямое подтверждение Утверждения A (§3): самоподкрепляющийся член $\alpha\Delta_f$ завышает наблюдаемую метрику.

### 7.3. Вычислительная проверка Теоремы 1 (T.1–T.7)

*Что делалось:* серия из семи проверок предсказаний β-канала на базовой симуляции.

| Проверка | Что измерялось | Результат |
|---|---|---|
| T.1 | $\lambda_{\max}(\hat\Sigma_k)$ во времени | монотонно убывает для всех кластеров ✓ |
| T.2 | глобальность аттрактора (23 инициализации) | все 23 → 0 ✓ |
| T.3 | внутрикластерное сжатие $\operatorname{tr}\Sigma_0$ | $2{,}737\to0{,}195$ ($-93\%$) ✓ |
| T.4 | Jaccard рекомендаций внутри кластера | $0{,}503\to0{,}862$ ($+71\%$) ✓ |
| T.5 | фазовый переход по $\varepsilon$ (ε-greedy) | не обнаружен (нужен бо́льший $\beta$/кластеризованные айтемы) ✗ |
| T.6 | косинусные расстояния петля vs `no_influence` | $0{,}11$ против $0{,}49$ ($\times4{,}4$) ✓ |
| T.7 | фрагментация однородной аудитории ($\eta^2$) | $\eta^2$: $0{,}455$ (петля) против $0{,}397$ ✓ |

Итог: 6 из 7 подтверждены; единственное исключение — T.5 (фазовый переход по $\varepsilon$ за пределами исследованного диапазона при медленном дрейфе). T.6 — прямое геометрическое свидетельство гомогенизации; T.7 показывает, что петля может и *поляризовать* однородную аудиторию (две группы расходятся).

### 7.4. Реальные данные (MovieLens-20M)

*Что делалось:* верификация H1/H3 вне синтетики. Подмножество топ-500 пользователей × топ-500 фильмов (плотность матрицы $61\%$), матричная факторизация ($d=16$), эмпирический генератор пользователей, 3 seeds. *Результат:* петля $\operatorname{tr}\hat\Sigma\ 0{,}174$ против `no_influence` $0{,}339$; $\mathrm{KL}\ 34{,}5$ против $19{,}7$ ($\times1{,}75$). Феномен воспроизводится с ещё более выраженным разрывом — он не артефакт GMM-генератора.

### 7.5. Чувствительность и границы петлевого сигнала (H7, H8)

- **H7 (класс модели и инициализация).** *Что делалось:* факторный grid $2\times3\times4\times30=720$ запусков: класс модели {MLP, MF} × инициализация {cold (случайная), warm (предобучение на истинных предпочтениях), warm_noisy} × 4 режима × 30 seeds; главный показатель — разрыв `closed_loop − static` по $\operatorname{tr}\hat\Sigma$ и $\mathrm{KL}$ с 95 % бутстрап-ДИ. *Результат:*

| Модель · инициализация | $\operatorname{tr}$-разрыв [95% ДИ] | $\mathrm{KL}$-разрыв [95% ДИ] | Сигнал |
|---|---|---|---|
| MLP · cold | $+0{,}139\ [0{,}011;0{,}269]$ | $-0{,}043\ [-0{,}287;0{,}199]$ | нет (KL незначим) |
| **MLP · warm** | $\mathbf{-0{,}162\ [-0{,}219;-0{,}109]}$ | $\mathbf{+0{,}793\ [0{,}641;0{,}945]}$ | **да** |
| **MLP · warm_noisy** | $\mathbf{-0{,}234\ [-0{,}282;-0{,}181]}$ | $\mathbf{+0{,}731\ [0{,}630;0{,}835]}$ | **да** |
| MF · cold | $+0{,}036\ [-0{,}014;0{,}087]$ | $-0{,}104\ [-0{,}193;-0{,}008]$ | нет |
| MF · warm | $-0{,}052\ [-0{,}114;0{,}009]$ | $-0{,}183\ [-0{,}283;-0{,}080]$ | нет |
| MF · warm_noisy | $-0{,}005\ [-0{,}072;0{,}056]$ | $+0{,}135\ [0{,}021;0{,}244]$ | слабый |

*Вывод:* warm-MLP **восстанавливает сигнал петли** (значим по обеим метрикам в согласованном направлении) — прямое подтверждение условия $\lVert J_k\rVert\neq0$. Различимость пользователей *необходима, но не достаточна*: MF с identity-init различает пользователей, но почти не «заостряет» выдачу (она уже близка к оптимуму), и сигнал не возникает — нужна и достаточная ёмкость модели.

- **H8 (устойчивость и границы).** *Что делалось:* фокус на warm-MLP, 20 seeds. **Часть A** (160 запусков): full (замена 5 %) vs frozen (замена 0 + отключение пересчёта центров). **Часть B** (≈1040 запусков): свип по одной оси за раз — $K\in\{1,2,3,5,8\}$, межкластерное расстояние, ширина $\sigma$, баланс весов (uniform / skewed $[0{,}6;0{,}3;0{,}1]$).

  *Результат A (гипотеза H8.1 отвергнута с обратным знаком):* при заморозке популяции $\mathrm{KL}$-разрыв падает с $+0{,}79$ до $-0{,}005$ (ДИ накрывает ноль) — режимы становятся неразличимы. Обновление аудитории не маскировало сигнал, а было его *необходимым условием*: при заморозке β-коллапс идёт до конца идентично в обоих режимах, и различие «насыщается».

  *Результат B (H8.2 подтверждена):* при $K=1$ сигнала нет ($-0{,}21$, значимо отрицателен); при $K\ge2$ значим (максимум $+0{,}79$ при $K=3$; $K=2$: $+0{,}56$; $K=5$: $+0{,}43$; $K=8$: $+0{,}35$). При перекосе весов (skewed) сигнал исчезает ($+0{,}02$, незначим): доминирующий кластер делает популяцию квазиодномодальной. По расстоянию и ширине сигнал значим всюду, но величина немонотонна (KL-разрыв максимален при *перекрытии*/размытости кластеров — там переформирование разброса доминирует в $\mathrm{KL}$).

### 7.6. Абляции: происхождение коллапса и роль переобучения

Две серии абляционных экспериментов установили *причину* эффектов.

**Диагностика происхождения коллапса** (≈340 запусков):
- *Эксперимент A (расширенный baseline):* 4 режима × 8 seeds с метриками концентрации (Gini, entropy, coverage). Показал: выдача концентрирована (Gini $\approx0{,}6$) *во всех* режимах, даже у случайной модели; истинное качество в `static` даже выше, чем в петле.
- *Эксперимент B (β-sweep) — ключевой:* $\beta\in\{0;0{,}001;0{,}005;0{,}02;0{,}05\}$ × 4 режима × 5 seeds. **При $\beta=0$ коллапса нет ни в одном режиме** ($\operatorname{tr}\hat\Sigma\approx30$, спад $\approx0\%$), включая петлю; при $\beta=0{,}005$ — максимум коллапса ($\approx0{,}8$) во всех режимах с $\alpha>0$. Прямое подтверждение Следствия 1.1.
- *Эксперимент C (α-sweep в `static`):* $\alpha\in\{0\ldots0{,}9\}$ без переобучения. При $\alpha\ge0{,}4$ коллапс достигает $0{,}83$–$1{,}24$ — *эквивалентно* петле. Переобучение не является источником коллапса.
- *Эксперимент D (факторный):* $(\alpha,\beta)\times$ режим (144 запуска). Эффект режима пренебрежимо мал по сравнению с эффектом $(\alpha,\beta)$.
- *Mechanism (12 конфигов):* изоляция компонентов. При `random serving` (softmax с высокой температурой ≈ равномерная выдача) коллапс не ослабляется, а **усиливается** до $99{,}6\%$ — все пользователи дрейфуют к среднему каталога (≈ началу координат).

  *Вывод:* коллапс $\operatorname{tr}\hat\Sigma$ универсален при $\beta>0,\alpha>0$ и порождён β-дрейфом по концентрированной выдаче, а не петлёй. Гипотеза H3 в формулировке «коллапс специфичен для петли» **отвергнута**; β-канал (Теорема 1) подтверждён.

**Абляция параметра дрейфа центров (проверка механизма петлевого канала).** *Что делалось:* воспроизведена конфигурация H7 (warm-MLP), факторный дизайн $2\times2$: {drift_alpha $=0{,}02$, $0{,}0$} × {петля, заморозка}, 30 seeds; $\mathrm{KL}$ раскладывался на член сдвига среднего и дисперсионный член.

| Метрика (разрыв петля − заморозка) | drift_alpha $=0{,}02$ | drift_alpha $=0$ |
|---|---|---|
| $\mathrm{KL}$ полный | $+0{,}789\ [0{,}658;0{,}927]$ | $+0{,}788\ [0{,}655;0{,}924]$ |
| — член сдвига среднего | $+0{,}002\ [-0{,}006;0{,}009]$ | $+0{,}002\ [-0{,}006;0{,}009]$ |
| — дисперсионный член | $+0{,}788\ [0{,}656;0{,}925]$ | $+0{,}788\ [0{,}653;0{,}930]$ |
| $\operatorname{tr}\hat\Sigma$ | $-0{,}162\ [-0{,}215;-0{,}111]$ | $-0{,}162\ [-0{,}213;-0{,}112]$ |

*Три вывода, все робастны:*
1. **Параметр дрейфа центров нерелевантен.** Его выключение меняет $\mathrm{KL}$-разрыв на $0{,}001$; агрегаты совпадают побайтово. (В коде он оказался «мёртвым»: его запись затиралась пересчётом центров из данных каждый шаг.) Прежнее представление, что дрейф центров — основной источник KL, неверно; параметр **исключён** из модели.
2. **Петлевой сигнал — в дисперсии, а не в сдвиге центра.** Весь $+0{,}79$ сосредоточен в дисперсионном члене; сдвиг среднего $+0{,}002$ незначим. Это прямо подтверждает механизм §6: петля действует на *ковариацию*, а не на положение центров.
3. **Эффект петли реален.** Петля даёт строго меньший $\operatorname{tr}\hat\Sigma$ ($-0{,}16$, значимо): переобучение дополнительно сжимает и переформирует разброс сверх универсального β-коллапса.

*Оговорка о знаке вклада в истинное качество:* в этой абляции истинное качество в петле ($0{,}314$) оказалось номинально даже *выше*, чем в заморозке ($0{,}301$); различие не проверялось на значимость. Знак прямого вклада петли в истинное качество **не установлен**.

### 7.7. Прямая проверка механизма петлевого канала: проба политики (опровержение)

Механизм §6 (Теорема 2) держится на одном предсказании: переобучение *заостряет* выдачу, сужая ковариацию показанного $C$ (в петле глубже, чем при заморозке). Раньше это лишь постулировалось и проверялось косвенно (через дисперсионный $\mathrm{KL}$ вкусов, §7.6). Здесь оно проверено **напрямую**.

*Что делалось.* **(А)** На каждом шаге измерялась ковариация фактически показанных top-$K$ векторов $\Sigma_\xi$ по кластерам, `closed_loop` vs `static` (warm-MLP, парно): сравнение по $\operatorname{tr}$, объёму, Loewner-порядку $\Sigma_\xi^{\mathrm{stat}}\succeq\Sigma_\xi^{\mathrm{closed}}$ и дисперсии $\mathcal D$; плюс свипы по adherence $\alpha$ и ширине кластера $\sigma$. Обнаружилось, что петлевой сигнал по вкусам **режимо-зависим** (есть лишь при запрете повторных показов — вынужденном исследовании), а именно там прямой замер $\Sigma_\xi$ испорчен *исчерпанием каталога* (показанное становится почти случайным). **(Б)** Поэтому — решающий тест, **проба политики**: после прогона берётся финальная модель (переобученная для `closed_loop`, замороженная warm — для `static`) и оценивается её top-$K$ по *полному каталогу* на фиксированных пробных пользователях (общие позиции для обоих режимов) — чистая концентрация политики, без артефактов исчерпания/оттока. В тех же прогонах фиксируется разрыв по вкусам — **позитивный контроль**.

*Результат — механизм опровергнут с позитивным контролем* (10 seeds, 95 % бутстрап-ДИ):

| метрика (closed − static, $t=T$) | значение | смысл |
|---|---|---|
| **вкусы** $\operatorname{tr}\hat\Sigma$ | $-0{,}158\ [-0{,}232;-0{,}079]$ ✓ | петля глубже сжимает вкусы (воспроизводит §7.5/§7.6) |
| **вкусы** $\mathrm{KL}(P_T\Vert P_0)$ | $+0{,}811\ [+0{,}527;+1{,}129]$ ✓ | петля дальше уводит вкусы (воспроизводит §7.5/§7.6) |
| **политика** $\operatorname{tr}$ top-$K$ (проба) | $+0{,}212\ [+0{,}111;+0{,}307]$ | top-$K$ переобученной модели **ШИРЕ**, не уже (closed $4{,}21$ vs static $4{,}00$) |
| показанное $\Sigma_\xi$, Loewner | держится лишь в **7 %** случаев | $\Sigma_\xi^{\mathrm{closed}}\preceq\Sigma_\xi^{\mathrm{stat}}$ **не выполняется** |
| показанное $\Sigma_\xi$, α-свип | разрывы н.з., нет тренда по $\alpha$ | самоподкрепление §6.5 не наблюдается |

*Вывод.* В одних и тех же прогонах петлевой след по вкусам воспроизводится в точности (trace $-0{,}16$, KL $+0{,}79$, оба значимы), а проба политики даёт **противоположное** предсказанному: переобученная политика *шире*, а не уже. Следовательно:

1. **Переобучение не заостряет выдачу.** Гипотеза Теоремы 2 (рост резкости $A\Rightarrow$ сужение $C$, бывшая Loewner-связка) **отвергнута** прямым измерением — ни в фактических показах (Loewner 7 %, разрывы н.з., нет тренда по $\alpha$), ни в чистой пробе политики (политика *шире*). Косвенный дисперсионный $\mathrm{KL}$-сигнал (§7.6) реален, но порождён **не** сужением ковариации выдачи.
2. **Петлевой след реален, но опосредован перенацеливанием, а не сужением.** Совместимая с данными интерпретация: переобученная модель *отслеживает* сдрейфовавших пользователей (рекомендует айтемы у их текущего положения), усиливая индивидуальные аттракторы при более широкой, но лучше центрированной выдаче; замороженная модель скорит по исходной геометрии и хуже соответствует сдрейфовавшей популяции, частично «возвращая» пользователей. Перенацеливание действует на **среднее** показанного (мишень), а не на его **разброс** — и это согласуется с тем, что эффект существует лишь при вынужденном исследовании. Прямое измерение перенацеливания вынесено в §11.

Честное опровержение естественного кандидат-механизма **сужает пространство гипотез**: центральный тезис (петля оставляет специфический след в геометрии вкусов) сохраняется и подтверждён позитивным контролем — меняется лишь объяснение механизма. *(Артефакты: `exp_2026-06-12_display_covariance.py`, `…b_dissociation.py`, `…c_policy_probe.py`; рисунок `DIAG_policy_probe_dissociation.pdf`.)*

### 7.8. Сводная таблица «теория ↔ эксперимент»

| Теоретическое утверждение | Эксперимент | Ключевое число | Вывод |
|---|---|---|---|
| Утверждение A (§3): observed завышено | H6 | observed $0{,}754\!\to\!0{,}686$ выше true $0{,}690\!\to\!0{,}662$ | ✓ |
| Теорема 1: коллапс при $\beta>0$ | абляция B | $\beta=0\Rightarrow\operatorname{tr}\hat\Sigma\approx30$ | ✓ |
| Теорема 1: универсальность по режиму | абляции C/D/Mech | `static`, $\alpha=0{,}7$: спад $97\%$ | ✓ (H3 уточнена) |
| Теорема 1: множитель $(1-\beta)$ за клик | механистика | $\sqrt{\operatorname{tr}\hat\Sigma_T/\operatorname{tr}\hat\Sigma_0}\approx0{,}17$ | ✓ |
| Теорема 1: монотонность по $\alpha$ | H4 | $0{,}268\,(\alpha{=}0)\to0{,}018\,(\alpha{=}1)$ | ✓ |
| Теорема 1: глобальный аттрактор | T.2 | $23/23\to0$ | ✓ |
| Петлевой след по вкусам ($\operatorname{tr}\hat\Sigma\!\downarrow$, $\mathrm{KL}\!\uparrow$) при $\lVert J_k\rVert\neq0$ | H7, проба политики §7.7 | $\operatorname{tr}$ $-0{,}16$, $\mathrm{KL}$ $+0{,}79$ | ✓ |
| Петлевой след — в ковариации вкусов, не в центре | абляция дрейфа | дисп. $+0{,}788$; сдвиг $+0{,}002$ незн. | ✓ |
| **Теорема 2: переобучение сужает выдачу $C$** | проба политики §7.7 | политика **шире** ($+0{,}21$); Loewner 7 % | **✗ опровергнуто** |
| Различимость пользователей необходима | H7 (cold-MLP) | $\mathrm{KL}$ $-0{,}04$ незначим | ✓ |
| Нужна структура $K\ge2$ | H8.2 | $K{=}1$: $-0{,}21$; $K\ge2$: $+0{,}35\ldots+0{,}79$ | ✓ |
| баланс кластеров необходим | H8.2 | skewed: $+0{,}02$ незначим | ✓ |
| открытая популяция необходима | H8.1 | frozen: $-0{,}005$ незначим | ✓ (H8.1 отвергнута) |
| ёмкость модели нужна | H7 (MF vs MLP) | MF: сигнала нет | ✓ |
| знак вклада в true-quality | абляция дрейфа | true петля $0{,}314$ vs $0{,}301$ | измерено, не установлен |
| Теорема 1 на реальных данных | MovieLens-20M | $\operatorname{tr}$ $0{,}174$ vs $0{,}339$; $\mathrm{KL}\times1{,}75$ | ✓ |

Из 15 проверявшихся утверждений основной части подтверждены 14 (единственное исключение — фазовый переход по $\varepsilon$, T.5).

---

## 8. Что доказано, что постулировано, что измерено

| Результат | Статус | Основание / граница |
|-----------|--------|---------------------|
| Утверждение A (загрязнение логов) | **доказано строго** | линейность $\eta_t$; завышение метрик — *условно* на выравнивании с $f_t$ (§3.4). |
| Теорема 1 (β-канал) | **доказано строго** | предпосылки (A1)–(A4); оценка с $M_k(0)$, в среднем + Марков. |
| Теорема 2 (петля сужает $C$ глубже заморозки) | **доказано в гауссовой модели** | при модельных допущениях §6.1–6.3 (гауссов каталог, квадратичный скор, KL-улучшение политики). |
| Связь $C\!\downarrow\Rightarrow$ глубже коллапс вкусов | **доказано** | Лемма D + шумовой пол Теоремы 1 (§5.5, §6.4). |
| Форма переобучения = KL-улучшение | **модельное допущение** | не следует из ERM (§6.3); косвенно поддержано дисперсионным следом §7.6. |
| Квадратичная вовлечённость = локальный Тейлор reward | **доказано** | §6.3, §6.7: квадратичная форма — локальное разложение кликового/BCE-сигнала, не примитив. |
| Существование внутреннего максимума reward по $v$ | **модельное допущение** | $\theta$ не имеет внутреннего максимума (§6.1); нужна компактность/штраф нормы/локальный Лаплас. |
| Самоподкрепление (геом. рост $A$) | **направление доказано; величина — с оговоркой** | §6.5, иначе $C\to0$ сильнее реальности. |
| Порог активации (§6.6) | **строг лишь изотропно** | в общем случае Loewner-монотонности нет (контрпример). |
| Условия активации $\lVert J_k\rVert\neq0$, $K\ge2$, баланс, открытость | **измерено и подтверждено** | H7, H8.1, H8.2. |
| Кластерный reward $\to H\succeq0$ из BCE | **доказано в модели** | §6.7: линейный/откалиброванный скор даёт $H\succeq0$; общий — проверяемое условие. |
| Заострение выдачи переобучением ($C^{\mathrm{closed}}\preceq C^{\mathrm{stat}}$) | **доказано в гауссовой модели** | §6.3–6.4 (Теорема 2); прямой замер $\Sigma_\xi$ — §7.7. |
| Локализация эффекта в дисперсии $\mathrm{KL}$ | **измерено и подтверждено** | абляция дрейфа: дисп. $+0{,}788$, сдвиг $+0{,}002$ незначим. |
| Знак вклада петли в true-quality | **измерено, не установлено** | true петля $0{,}314$ vs $0{,}301$, не проверено. |

**Главный методологический тезис.** β-канал — строгая теорема. Петлевой канал формализован в специальной гауссовой модели (кластерный reward $\to$ KL-улучшение политики $\to$ заострение выдачи глубже, чем заморозка), с микрообоснованием кривизны через BCE (§6.7); его условия активации измерены, а локализация петлевого следа в дисперсионной части $\mathrm{KL}$ подтверждена напрямую (§7.6). Прямой замер сужения выдачи $C$ и его границы вынесены в §7.7. Все границы выписаны явно; разделение «доказано / постулировано / измерено» сохраняется по всему документу.

---

## 9. Итоговая причинная цепь по звеньям

Скрытая петля — это цепь, каждое звено которой имеет свой статус:

| Звено | Переход | Статус и основание |
|-------|---------|--------------------|
| (i) | загрязнение логов $\Rightarrow$ переобучение на $\eta_t$ | по построению ($\eta_t=(1-\alpha)\theta+\alpha f_t$, §3.1) |
| (ii) | переобучение $\Rightarrow A_{k,t}\!\uparrow$ | доказано в гауссовой модели (§6.3); $H\succeq0$ обоснована через BCE (§6.7) |
| (iii) | $A_{k,t}\!\uparrow\ \Rightarrow C_{k,t}\!\downarrow$ (и $C^{\mathrm{closed}}\preceq C^{\mathrm{stat}}$) | доказано (Лемма C, Теорема 2); прямой замер $\Sigma_\xi$ — §7.7 |
| (iv) | $C_{k,t}\!\downarrow\ \Rightarrow$ ниже шумовой пол $\Rightarrow$ глубже коллапс вкусов | доказано (матричная рекурсия $S$ + §5.5, §6.4); измерено (дисперсионный член $\mathrm{KL}$, §7.6) |
| (v) | глубже коллапс $\Rightarrow$ ещё концентрированнее логи | феноменологическое допущение (P-exp, §3.5) — *не доказано* |

β-канал (Теорема 1) гарантирует стягивание разнообразия **даже без переобучения** и универсально по режиму. Переобучение добавляет механизм усиления: сужение $C$ (Теорема 2) опускает пол β-коллапса, но активирует петлю лишь персонализированный режим (§6.6, $\nabla_u m_A\ne0$) при пороге $\sigma_u^2<\tfrac{1+a\psi}{2a}$.

**Прикладной критерий.** Риск оценивать по совместной динамике $\alpha=ps$, ковариации вкусов $S_{k,t}$, резкости $A_{k,t}$ и ковариации выдачи $C_{k,t}=\Sigma_\xi$ — не по observed-метрикам по отдельности.

---

## 10. Прикладные следствия и чек-лист риска

### 10.1. Почему высокие метрики скрывают деградацию

Observed-метрика **завышена относительно true**, когда оцениваемое ранжирование коррелирует с $f_t$ (например, при оценке на собственной выдаче): самоподкрепляющийся член $\alpha\Delta_f$ (§3.3) поднимает observed выше true. Подтверждено H6: observed $0{,}754\to0{,}686$ держится выше true $0{,}690\to0{,}662$.

### 10.2. Чек-лист риска петли

Петлевой эффект — «произведение» нескольких множителей: если хоть один равен нулю, патологии нет. Достаточно убрать любой один множитель, чтобы петлю обезвредить.

| № | Условие | Символ | Если нарушить — | Что мерить / ручка |
|---|---------|--------|------------------|--------------------|
| 1 | модель персонализирует | $\lVert J_k\rVert\neq0$ | петле нечего затачивать | $\lVert J_k\rVert$ из весов; cold-start безопаснее |
| 2 | аудитория неоднородна | $K\ge2$ | нечего перекашивать | сегментированность аудитории |
| 3 | логи загрязнены | $\alpha=ps>0$ | петли нет вовсе | **понижать $p$ или $s$** (исследовательские показы, взвешивание навязанных кликов) |
| 4 | выдача всё ещё заостряется | $\sigma_\xi^2\!\downarrow$ | эффект насыщается | следить за $\sigma_\xi^2$ показанного во времени |
| — | резкость ранжирования (усилитель) | зазор top-$L$ | размытие замедляет коллапс | exploration / diversity-aware (H5) |

> **Контринтуитивный вывод.** Чем *лучше* модель персонализирует, тем она *уязвимее* к петле: хорошая персонализация — это и польза, и канал загонки в эхо-камеры. Главная ручка вмешательства — понижение загрязнения $\alpha=ps$: оно ослабляет и петлевой канал, и завышение метрик одновременно.

### 10.3. Интервенции

(1) реже переобучать (меньше $\kappa$ — меньше множитель $1+\kappa\alpha$ в §6.5); (2) diversity-aware top-$L$ (поднимает $C_A$ и остаточный уровень разброса; в H5 даёт +53 % к сохранению дисперсии при том же качестве); (3) штраф на резкость $A$ / якобиан; (4) мониторинг разрыва observed–true и динамики $\operatorname{tr}\hat\Sigma$ как раннего индикатора (снижение видно задолго до падения CTR).

---

## 11. Границы применимости и дальнейшая работа

**Что осталось неустановленным.** (а) *Знак вклада петли в истинное качество* мал и не определён (§7.6) — нужны бо́льшие горизонты и доверительные интервалы. (б) *Абсолютная величина* сужения (пол $\sim0{,}8$, не $0$) расходится с идеализированной неограниченной рекурсией §6.5 — требуется ограничивающая гипотеза (член забывания). (в) Модельные допущения §6 (KL-форма переобучения, существование внутреннего максимума скора) проверены косвенно через предсказания; прямое извлечение резкости $A$ из весов обученной модели уточнило бы соответствие.

**Направления развития.** (1) Ввести медленную динамику самих истинных предпочтений (сейчас $\theta$ статично) — это нужно для идентифицируемости деконволюции наблюдаемого и истинного сигналов. (2) Прямо извлекать $\lVert J_k\rVert$ и резкость $A_{k,t}$ из весов обученной модели как операционную метрику риска. (3) Раздельный свип по usage $p$, adherence $s$ и периоду $T_{\mathrm{ret}}$ (сейчас $p$ и $s$ слиты в $\alpha$). (4) Антиколлапсная интервенция, нацеленная на *форму* ковариации (а не только на $\operatorname{tr}$): проверить, удерживает ли diversity-aware-выдача изотропность спектра, а не только объём. (5) Открытый приток пользователей (новички из исходного несжатого распределения) — баланс «сжатие против обновления».

**Итог.** Двухканальная модель согласована с данными: универсальность β-канала, заострение выдачи переобучением, локализация петлевого следа в дисперсионной части $\mathrm{KL}$ и его условия активации подтверждены численно и на реальных данных. Центральный тезис — *петля обратной связи оставляет специфический, отделимый от универсального дрейфа след в геометрии ковариации* — установлен в рамках исследованной модели и очерченных границ применимости.
